# Operator 与 CRD

> 大家好。前面讲完观测与日志；这一章讲**把 runbook 变成控制器**——CRD 只登记类型，Operator 才持续对账。
> 开讲之前，先带大家回到几个现场：集群里能 `kubectl get gameserver`，改 spec 毫无反应；有人要把开服脚本「云原生化」成 15s 调和一次的 Operator；另有人删了管云上 RDS 的 CR，十分钟还在 Terminating，准备手抠 finalizer。手都别动。
> 这一章讲完，你会知道这几个人各自错在哪——没有控制器改 spec 没反应、不是什么都该写成 Operator、手抠 finalizer 会留云孤儿。
> **本章回答的问题**：CRD 与 Operator 两件套；Observe → Diff → Act；Terminating / conversion / 升 CRD；Operator 挂了影响面。
> **本章不讲**：声明式控制循环的通用纪律 → [01 集群架构](../01-集群架构/)；准入 Webhook 焊死写路径 → [03](../03-API-Server/)；Chart / release → [22](../22-Helm与Kustomize/)；Git 当期望、Argo 调和 → [23](../23-GitOps-ArgoCD与Tekton/)；工作负载选 Deploy/STS → [05](../05-工作负载控制器/)。不背 kubebuilder 脚手架，不讲 OpenAPI schema 全集。

**标记**：🎯重点 · 🔥难点 · ⭐常考 · 🔧实操

**怎么听这场分享**：先认主线三步——① 两件套（CRD 注册，控制器执行）；② 循环（Observe → Diff → Act）；③ 卡死（finalizer / conversion）。口诀先埋一句：⭐ **CRD 只登记类型；没有控制器，etcd 里那坨 JSON 谁也不会去执行**。下面按节展开。

```mermaid
flowchart LR
    a[1 概念] --> b[2 架构]
    b --> c[3 原理]
    c --> d[4 部署]
    d --> e[5 配置]
    e --> f[6 命令与操作]
    f --> g[7 排障]
    g --> h[8 常考]
```


**本章主线（控制器就按这三步想）：**

1. **两件套**：CRD 注册类型；Operator / 控制器才执行 spec。
2. **循环**：Observe → Diff → Act；期望在 spec，结果写 status。
3. **卡死**：Terminating 看 finalizer；升 CRD 看 conversion 和存量兼容。

**读完你能：** 白板上画出 Observe → Diff → Act；判断该写 Operator 还是 Helm/Job；会看 status、选主、finalizer；升 CRD 知道 conversion；Operator 挂了能说出影响面。

## 本章目录

- [1. 基本概念](#1-基本概念)
- [2. 架构图](#2-架构图)
- [3. 原理](#3-原理)
- [4. 安装部署](#4-安装部署)
- [5. 核心配置](#5-核心配置)
- [6. 命令与操作](#6-命令与操作)
- [7. 生产排障](#7-生产排障)
- [8. 必会 · 常考](#8-必会--常考)
- [合上书](#合上书问自己)

---

## 1. 基本概念

人不能一直 `kubectl`。扩分片、修主从、续证书，如果靠值班手点，漏一次就是事故。Operator 把这份 runbook 变成和 Deployment 同一类东西：**持续 Reconcile**。

**值班里什么时候碰到**：`kubectl get` 出现自定义 kind；改了 spec 子资源不动；删 CR 一直 Terminating；手改 STS 副本过一会儿又变回去。

两件套不要混：

| | 干什么 | 没有它会怎样 |
|--|--------|--------------|
| **CRD** | 让 API Server 认识一种新 kind，对象进 etcd | `kubectl get` 都不认识这个类型 |
| **Operator / 控制器** | Watch spec，对比集群实际，Act，写 status | etcd 里那坨 JSON **谁也不会去执行** |

CRD 只注册类型。没有控制器，它就是一坨 YAML。有控制器，才是那扇门。

大家想想：能 `kubectl get gameserver` 就说明 Operator 在干活吗？——⭐ **不一定**。get 通只说明 CRD 注册了；改 spec 没反应，才是控制器挂了或根本没装。

```yaml
apiVersion: game.example.com/v1
kind: GameShard
metadata:
  name: battle-1001
spec:
  replicas: 2
  version: "1.2.1"
status:
  conditions:
    - type: Ready
      status: "True"
      message: "STS battle-1001 ready"
```

期望在 spec，给人看的结果在 status。`kubectl get gameshard` 能扫 Ready/Failed，比翻 Operator 日志快。status 条件学内置 Deploy 的 Available/Progressing，不要发明一套只有作者懂的字段。状态不要只放 Operator 内存——重启就丢，别人也看不见。

业务、kubelet、Controller **禁止直连 etcd**。直连绕过 RBAC、准入、审计；写坏的对象会被控制器当成期望去执行（01、03）。Informer 缓存仍从 API List-Watch，写入仍走 API，不算绕过。

**打个比方**：CRD 是工商登记「允许你开这类店」；Operator 是店长每天对账——货架（STS）和账本（spec）不一致就改回去。只有执照没有店长，店里永远空着。

> **记住**：CRD 只注册类型；没有控制器，etcd 里那坨 JSON 谁也不会去执行。

**面试怎么说**：「CRD 登记 kind，Operator 持续 Reconcile；期望在 spec，结果写 status；没有控制器改 spec 没反应。」

---

本节对应练习题 Q1、Q2。

## 2. 架构图

好，概念有了。接下来看架构——后面的内容都对着这张图。

先把整张图钉住。后面安装、命令、排障都对着这张图说话。

```mermaid
%%{init: {"flowchart": {"padding": 18, "nodeSpacing": 28, "rankSpacing": 50}, "theme": "base"}}%%
flowchart TB
    subgraph clients ["① 只写期望"]
        direction LR
        human[人 / Git apply CR]
        helm[Helm 只提交 YAML]
    end

    subgraph cp ["② 控制面大门"]
        api["kube-apiserver"]
        etcd[("etcd /registry")]
        crd[CRD 登记 kind]
        api --- crd
        api --> etcd
    end

    subgraph op ["③ Operator · 门"]
        inf[Informer 缓存]
        q[WorkQueue 去重]
        rec[Reconcile]
        lead[Leader 才 Act]
        inf --> q --> rec
        rec --> lead
    end

    subgraph child ["④ 子资源 · 真正跑业务"]
        sts[STS / Deploy]
        svc[Service]
        pvc[PVC]
    end

    human --> api
    helm --> api
    rec -->|"经 API 建/改/删"| sts
    rec --> svc
    rec --> pvc
    rec -->|"patch status"| api
    api -->|"List-Watch"| inf

    class human,helm client
    class api,crd api
    class etcd etcdN
    class inf,q,rec,lead opN
    class sts,svc,pvc childN

```

**读图三句话：**

1. **没有箭头从 Operator 指向 etcd**——只连 API；apply CR 只是记账，真正建 STS 是下一轮 Reconcile。
2. **Operator 不起容器**——Scheduler 写 nodeName，kubelet 调 CRI，和内置 Deployment 控制器同一条分界（01）。
3. **排障从 CR status 列进**——卡在 ① 看 CRD/Operator 在不在；卡在 ④ 走 17，不是改 Operator。

一次 apply 到 Ready，你眼睛看到的：

```mermaid
flowchart LR
    A[① etcd 有 CR] --> B[② Operator Watch]
    B --> C[③ 建 STS/SVC/PVC]
    C --> D[④ kubelet 起容器]
    D --> E[⑤ status.Ready]

    class A,B,C a
    class D,E b
```

| 卡在 | 你看到的 | 先看 |
|------|----------|------|
| ① 只有 CR | get kind 有，sts 无 | Operator 在不在、CRD 是否装过 |
| ②③ Act 失败 | Operator 日志 forbidden / panic | RBAC、queue、leader |
| ④ 子资源没 Ready | STS 在、Pod 不 Running | 走 17，不是改 Operator |
| ⑤ status 空 | 子资源好了，READY 列空白 | 能不能 patch status |

手改 STS 副本从 2 改到 5，过一会儿又变回 2——这是 Reconcile，不是 bug。期望在 CR 的 spec。要扩容改 CR。

---

本节对应练习题 Q1、Q3。

## 3. 原理

带着这张图，咱们进原理——难点放慢讲。

### 3.1 和解循环：Observe → Diff → Act

🔥 **和解（Reconcile）** = 持续对账，不是 apply 一次就结束。手改子资源（STS 副本）会被下一轮改回去——这是特性，不是 bug。

**一句话：** 持续对账，不是 apply 一次就结束；手改子资源下一轮会被改回去。

**打个比方**：像银行自动对账——你手工改了一笔账，系统每 15 秒又把账本拉回到 CR 写的数。

**现场走一遍**（改 spec 后 STS 没动）：

1. 跳板机改 CR：`kubectl patch gameshard battle-1001 --type merge -p '{"spec":{"replicas":3}}'`
2. 立刻看子资源：`kubectl get sts -l app=battle-1001`
3. 屏幕 replicas 仍是 2 → **别先 scale STS**，去看 Operator：
4. `kubectl -n op-system logs deploy/game-operator --tail=50`
5. 若看到 `forbidden: cannot patch statefulsets` → RBAC 缺权限，不是 kubelet 坏了
6. 修 RBAC 后下一轮 Reconcile，STS 变 3，status 写 Ready

```mermaid
flowchart LR
    A[Observe 读 spec 与子资源] --> B[Diff 和期望比]
    B --> C[Act 建/改/删]
    C --> D[写 status]
    D --> E{一致?}
    E -->|是| F[继续 Watch]
    E -->|否 / 错| G[requeue 限速再来]

    class A,B a
    class C,D b
    class F c
    class G d
```

**输出解读**（`kubectl get gameshard battle-1001 -o wide`）：

```text
NAME          READY   VERSION   AGE
battle-1001   True    1.2.1     2d
```

| 列 | 正常 | 异常 |
|----|------|------|
| READY | True | False/空 → 看 status.conditions 或 Operator 日志 |
| 改 spec 后 STS 不变 | — | 先 Operator，不是 kubelet |

关键口径：Level-triggered，丢 Watch 不可怕；Act 必须**幂等**；requeue 限速；bug 以调和频率放大——某次写副本 0，15s 一次把所有区服缩没。

游戏开服 CR 把「起 STS + 注册路由」写成 Operator，半成功状态上反复 Act，合服要人工确认的步骤会被自己循环。这类任务用 Job / 流水线，不要常驻和解。

**面试怎么说**：「和解是 Observe-Diff-Act 持续循环，level-triggered 必须幂等；手改 STS 会被改回去，要改 CR spec。」

> **记住**：和解是持续对账，不是「apply 一次就结束」。手改子资源会被下一轮改回去——这是特性。

### 3.2 Informer、队列、选主

**一句话：** 多副本 Operator 必须选主，否则两个 Reconcile 同时 Act 子资源抖。

**打个比方**：两个店长同时改货架——一个摆 3 个苹果一个摆 5 个，顾客看见的数量来回跳。

**现场走一遍**（双副本 STS 副本数来回跳）：

1. `kubectl -n op-system get deploy game-operator` → replicas: 2
2. `kubectl -n op-system get lease`：

```text
NAME              HOLDER          AGE
game-operator     pod-abc-xyz     3d
```

3. 若 **两个 Pod 都在写日志里有 Act** → 没开 leader election
4. 开选主后只有一个 HOLDER；standby 只 Watch 不 Act
5. STS 副本稳定

```mermaid
flowchart TB
    api[API Server] -->|List-Watch| inf[Informer 本地缓存]
    inf --> q[WorkQueue 去重限速]
    q --> rec[Reconcile]
    rec --> lead{是 Leader?}
    lead -->|是| act[Act]
    lead -->|否| wait[standby]

    class api,inf,q a
    class act b
    class wait c
```

**输出解读**：Informer 不是直连 etcd，仍从 API List-Watch。WorkQueue 把同 key 多次变更收成一次。单副本挂了：调和停，已有 STS 还在——和 Controller Manager 挂了同类（01）。

**面试怎么说**：「Informer 缓存走 API List-Watch；多副本必须 leader election，不能双活同时 Reconcile。」

### 3.3 CRD 版本与 conversion

**一句话：** storage 版本只能一个；改结构必须 Webhook 或一次性迁完，不能顺手删旧字段。

**打个比方**：图书馆只存一种装订版（storage），读者可以借旧版封面（served），借还时柜台做翻译（conversion）。

**现场走一遍**（升 CRD 后创建 CR 被拒）：

1. `kubectl apply -f new-crd.yaml` 后，`kubectl create -f gameshard.yaml` 报错
2. 屏幕：

```text
Error from server: error when creating "gameshard.yaml":
gameshards.game.example.com "battle-1001" is invalid:
spec.shardId: Required value
```

3. 说明新 schema 加了必填字段，**已有对象也变非法** → 调和全停
4. 正确路径：先上 conversion webhook，或双写；CRD 与 Operator 镜像**同窗口**升级
5. `kubectl get crd gameshards.game.example.com -o jsonpath='{.spec.versions[*].name}{"\n"}'`

| 字段 | 含义 |
|------|------|
| `served: true` | 这个版本的 API 路径能用 |
| `storage: true` | **只能一个**；etcd 里存的形态 |
| `deprecated: true` | 客户端该迁走 |
| conversion `None` | 多版本只许改注解/label 这类兼容字段 |
| conversion `Webhook` | v1alpha1 ↔ v1 字段对不上时必须 |

**输出解读**：`storedVersions` 里还有旧版本时，不要立刻从 CRD 删那个版本。conversion webhook 挂了：旧版本读写会失败。卡住创建 CR：先看校验 Webhook 和 CRD schema。

**面试怎么说**：「served 能读几个版本，storage 只有一个写 etcd；改结构要上 conversion webhook；升 CRD 删字段会把存量 CR 打成非法。」

> **记住**：升 CRD 别删正在用的字段；storage 版本只有一个；CRD 和 Operator 镜像必须同窗口。

### 3.4 Finalizer、owner、RBAC

🔥 Terminating 卡住，有人手抠 finalizer——大家想想会怎样？如果这个 finalizer 管的是云上 RDS，抠掉 = **集群里对象没了，云上孤儿还在继续计费**。

**一句话：** Terminating 先看 Operator 清外部资源，第一刀不要抠 finalizer。

**打个比方**：退房要还钥匙（finalizer）——你硬把登记删了（抠 finalizer），云上的 RDS 还在计费。

**现场走一遍**（删 CR 十分钟还在 Terminating）：

1. `kubectl get gameshard rds-cluster-01` → STATUS 空，但 `metadata.deletionTimestamp` 有值
2. `kubectl get gameshard rds-cluster-01 -o yaml | grep -A5 finalizers`：

```text
finalizers:
  - rds.example.com/cleanup
```

3. `kubectl -n op-system get po -l app=game-operator` → CrashLoopBackOff
4. **先拉起 Operator**，看它日志里云 API 403 还是正在删实例
5. 外部确认已清、业务允许，Operator 才会摘 finalizer；对象才消失

```mermaid
flowchart TB
    del[kubectl delete CR] --> ts[deletionTimestamp]
    ts --> fin{finalizer 还在?}
    fin -->|是| ext[Operator 清外部 RDS/DNS/S3]
    ext --> wait[卡住：看 Operator 和云 API]
    fin -->|否| gc[GC 删 owner 的子资源]
    gc --> gone[对象从 etcd 消失]

    class wait bad
    class gone ok
```

**输出解读**：

| 机制 | 管什么 | 缺了会怎样 |
|------|--------|------------|
| **finalizer** | 集群外（云 DNS、S3、RDS） | 手抠 → 云上孤儿 |
| **ownerReferences** | 集群内 STS / PVC / Service | 缺 → CR 没了 STS 还在；配错 → GC 删错 |

RBAC 禁止 `*`。最小是本 CR 的 get/list/watch/update/patch/status，外加它创建的那几类子资源。Namespace 一直 Terminating：往往 NS 里还有带 finalizer 的 CR。

**面试怎么说**：「Terminating 看 finalizer 和 Operator；finalizer 管集群外，owner 管集群内 GC；手抠 finalizer 会留云孤儿。」

---

本节对应练习题 Q3、Q4、Q5、Q10。

## 4. 安装部署

生产怎么选：

| 项 | 选 | 不选 |
|----|----|------|
| 要不要写 | 持续决策 + 有人 oncall + 上游没有 | 装一次 YAML、开合服批任务 |
| 形态 | 上游成熟 Operator，或自研有人测 CRD 升级 | 没人看的「云原生化脚本」 |
| 副本 | ≥2 + **leader election** + PDB | 双活同时写；单副本无 PDB |
| 权限 | 本 CR + 几类子资源 | cluster-admin / `*` |
| 安装 | Helm/GitOps 装 Operator；运行中决策才是它的活 | 和 Argo 同时手改子资源（22） |

落地你实际看到什么：

| 对象 | 内容 |
|------|------|
| `kubectl get crd` | 类型登记了没有 |
| Operator Deployment + leader lease | 门在不在、谁在 Act |
| CR status.conditions | 给人看的结果 |
| ownerReferences 在 STS 上 | 删 CR 时 GC 认不认 |
| Validating / conversion Webhook | 写路径和升版本依赖 |

验收：能 get kind；改 spec 后 STS 跟着变；status 有 Ready；删 CR 子资源走 GC（集群外有 finalizer 会先清）。不要只看 Operator Pod Running。

引入上游 Operator 前先问三句：它会创建哪些 STS/PVC/Job？失败日志在哪？CRD 升级谁测？答不出就不要把生产数据库交给它。

游戏集群：日批开服 / 合服用 Job + 工作流（08 游戏专题），不要 15s 调和。区服扩分片、证书续期、有状态中间件 failover，才配 Operator。硬把 Ansible 翻译成 Go，收益为负。

---

本节对应练习题 Q11。

## 5. 核心配置

只动有证据的项。

| 项 | 生产怎么用 | 改错会怎样 |
|----|------------|------------|
| leader election | 多副本必须开 | status / 子资源双写抖动 |
| 调和周期 / requeue | 外部 API 用 `RequeueAfter`，不要死循环 15s | 打满云 API；bug 放大 |
| Webhook failurePolicy | 校验可 Fail；升级窗口 conversion 必须活 | Fail + 挂 = 焊死创建 |
| CRD conversion | 改结构就上 Webhook；否则 None | 已有 CR 变非法 |
| storage 版本 | 一次只迁一个；迁完再关旧 served | storedVersions 残留删不掉 |
| RBAC | 拆 status 的 patch | 能建 STS 不能写 Ready 列 |
| PDB / 反亲和 | Operator 自己也要 | drain 一台调和全停 |
| FieldManager | GitOps 下不要和 Argo 抢（22） | 来回漂移 |

status 条件：Ready / Progressing / Degraded，带 `lastTransitionTime` 和 message。不要只放内存。

> **记住**：安装形态用 Helm/GitOps；**运行中持续决策**才配 Operator。三者所有权只能有一个。

---

本节对应练习题 Q2、Q8、Q13。

## 6. 命令与操作

好，配置记住了。接下来是值班真敲的命令。

### 6.1 命令速查

值班先这几条：

```bash
kubectl get crd | grep game
kubectl get gameshard -A -o wide          # 先看 status 列
kubectl -n op-system logs deploy/game-operator --tail=100
kubectl -n op-system get lease            # 谁是 leader
kubectl get sts,svc,pvc -l app=battle-1001
kubectl get gameshard battle-1001 -o yaml | grep -A20 finalizers
kubectl get validatingwebhookconfiguration
kubectl get crd gameshards.game.example.com -o yaml   # served / storage / conversion
```

| 目的 | 命令 | 看什么 |
|------|------|--------|
| 类型在不在 | `kubectl get crd` | 只有 CRD 没有控制器 = 改 spec 无反应 |
| CR 总览 | `get <kind> -o wide` | Ready/Failed、message |
| 控制器 | `logs deploy/<operator>` | forbidden、panic、queue |
| 选主 | `get lease` | 双副本谁在 Act |
| 子资源 | `get sts,svc,pvc -l ...` | owner 在不在 |
| Webhook | `get validatingwebhookconfiguration` | 创建被拒先看这个 |
| CRD 版本 | `get crd <name> -o yaml` | served / storage / conversion |

创建被拒：先 schema 和 Webhook，不要先改业务镜像。spec 改了 STS 不动：先 Operator 日志，不要先 `kubectl scale sts`。

### 6.2 何时写、何时不要写

Operator 把 runbook 变成每 15s 跑一次的代码。**不是银弹。**

```mermaid
flowchart TB
    q[要不要写 Operator]
    q --> a[只渲染 YAML / 装一次]
    a --> helm[Helm / Kustomize / GitOps]
    q --> b[开服、合服、一次性迁库]
    b --> job[Job / 流水线，失败可重跑]
    q --> c[持续决策且有人看、上游没有]
    c --> op[才写或引入]
    q --> d[没人看 / 没人测 / 上游已有]
    d --> no[立项就是负债]

    class helm,job ok
    class op mid
    class no no
```

自研成本：CRD 版本与 conversion、校验、status 条件、最小 RBAC、升级 CRD 不丢字段。没人 oncall 这个 Operator：Reconcile 报错没人看，比偶尔跑 Ansible 更危险。

和 Helm 的分界：Helm 装的 Redis 起不来，看 release 和渲染 YAML（21）；Operator 管的 Redis 起不来，先看 CR status 和 Operator 日志。

### 6.3 升 CRD

1. 列现网用了哪些字段、哪些版本 served。
2. 改结构 → 先上 conversion webhook，再加新版本为 served；storage 仍先留旧。
3. **CRD 与 Operator 镜像同窗口**；新字段旧控制器看不见等于没人执行。
4. 对象都按新 storage 写回后，再切 storage，最后 `served: false` 旧版本。
5. 不要删正在用的字段当「清理」。

回滚：conversion 还在时可以再 served 旧版本。已经删字段、已有对象非法，回滚 CRD YAML 也不一定救得回来——升级前要有 CR 清单备份。

### 6.4 Terminating

1. CR 一直 Terminating，Operator Pod 不在 → 先拉起 Operator，看它清外部清到哪。不要第一刀就抠。
2. Operator 在，日志里云 API 403 → 修权限或控制台删实例，确认外部已无、业务允许，再补救，留变更记录。
3. CR 没了，STS 还在 → 缺 ownerReferences。
4. Namespace 一直 Terminating → 先列 NS 里带 finalizer 的 CR。

---

本节对应练习题 Q6、Q14。

## 7. 生产排障

好，命令会了。接下来按现象定责——先做什么、别做什么。

Operator 挂了：**已有子资源还在，只是不再调和**——和 01 里 Controller Manager 挂了同类。不要人肉改它管的 STS，下一轮 Reconcile 会改回去。不要重启还活着的战斗服。

```mermaid
%%{init: {"flowchart": {"padding": 16, "nodeSpacing": 28, "rankSpacing": 48}, "theme": "base"}}%%
flowchart TB
    subgraph gate ["先分叉"]
        A[现象] --> B{CR / 控制器 / 子资源?}
    end

    subgraph cr ["CR"]
        B -->|Terminating| C[Operator 在不在 · 云 API]
        B -->|创建被拒| D[Webhook / CRD schema]
        B -->|status 空| E[能否 patch status]
    end

    subgraph ctrl ["控制器"]
        B -->|spec 改了没反应| F[日志 · RBAC · leader · queue]
        B -->|双副本打架| G[没选主]
    end

    subgraph child ["子资源"]
        B -->|改了又回去| H[改 CR 别改 STS]
        B -->|STS 在 Pod 不 Running| I[走 17]
    end

    class C,D bad
    class H,I ok
    class F,G,E warn

```

现场顺序：

```bash
kubectl get crd | grep game
kubectl get gameshard -A -o wide
kubectl -n op-system logs deploy/game-operator --tail=100
kubectl -n op-system get lease
kubectl get sts,svc,pvc -l app=battle-1001
```

| 现象 | 先做什么 | 不要做什么 |
|------|----------|------------|
| 只有 CR 没有 STS | Operator 在不在 | 当 kubelet 坏了 |
| spec 改了没反应 | 日志、RBAC、leader | 先 scale STS |
| Terminating | 控制器和外部资源 | 第一刀抠 finalizer |
| 创建被拒 | Webhook / schema | 换业务镜像 |
| 子资源乱、改了又回去 | 改 CR | 关 Operator 硬推 |
| status 一直空 | patch status RBAC | 只翻业务日志 |
| 双副本打架 | 开选主 | 再加副本「更 HA」 |
| Operator 挂了 | 拉起控制器 | 重启游戏进程 |
| CRD 升级后全停 | conversion / 非法字段 | 再删一轮字段 |

给 Operator 配 PDB 和反亲和。Webhook 是写路径依赖（03）。GitOps 下谁是 FieldManager 要分清（22），别和 Argo 同时手改。

---

本节对应练习题 Q5、Q6、Q9、Q12、Q14。

现在再看开篇那几句——「改 spec 没反应就重装」「开服脚本全写成 Operator」「手抠 finalizer」——你知道该怎么拦住他们了：先确认控制器在不在、先问该不该写、Terminating 先看 finalizer 别硬抠。

## 8. 必会 · 常考

遮住「口径」列自问自答，卡壳的行回去重读对应小节——能讲出来才算会，看着答案点头不算。

白板和追问高频，能一口回：

| 说法 | 对错 | 口径 |
|------|:----:|------|
| 有 CRD 就能自愈 | 错 | 没有控制器只是 etcd 里的 JSON |
| Operator 挂了游戏服立刻没了 | 错 | 子资源还在；停止调和 |
| Operator 直接起容器 | 错 | 只改 API 对象；kubelet 起容器 |
| 多副本双活同时 Reconcile 更稳 | 错 | 必须选主 |
| 开服脚本都该写成 Operator | 错 | 批任务用 Job；bug 按 15s 放大 |
| 装一次 Redis 也该写 Operator | 错 | Helm/GitOps |
| 手改 STS 是调参 | 错 | 下一轮改回去；改 CR |
| Terminating 先抠 finalizer | 错 | 先看控制器和外部 |
| Informer 缓存等于直连 etcd | 错 | 仍走 API List-Watch |
| 升 CRD 可以顺手删旧字段 | 错 | 已有对象变非法 |
| 多个 storage 版本同时写 etcd | 错 | storage 只能一个 |
| conversion 可有可无 | 错 | 改结构必须 Webhook 或一次性迁完 |
| RBAC 先 `*` 跑起来再说 | 错 | 爆炸半径全集群 |
| status 放内存就行 | 错 | 重启丢失，kubectl 看不见 |
| Helm 装的和 Operator 管的排障一样 | 错 | 前者看 release，后者看 CR status |

数字：选主必须；调和常见秒～十几秒级；CRD 版本 v1alpha1 → v1；storage **1 个**；审计/Webhook 失败策略见 03；PDB 给 Operator 自己也要。

易混：CRD vs Operator；finalizer vs ownerReferences；level-triggered vs 一次性 Job；Helm 安装 vs 运行中决策；conversion None vs Webhook；Webhook 校验失败 vs schema 失败；改 STS vs 改 CR。

---

本节对应练习题 Q1～Q14。

---

## 合上书，问自己

合上书自检（题号对齐练习题）：

- [ ] CRD 和 Operator 是什么关系？只有 CRD 会怎样？（Q1）
- [ ] 什么场景该写 Operator？为什么不是银弹？（Q2、Q8）
- [ ] 和解循环怎么转？手改 STS 为什么会被改回去？（Q3）
- [ ] 升 CRD 删字段为什么危险？conversion 什么时候必须有？（Q4、Q12）
- [ ] CR 一直 Terminating，能不能手抠 finalizer？（Q5）
- [ ] Operator 挂了游戏服还在吗？spec 没反应按什么顺序查？（Q6、Q9、Q14）

六题都能口述，这一章就过了。终极测试：拦住开篇那位要手抠 finalizer 的人。下一章讲 Helm / Kustomize——最后都变成普通 YAML。
