# 项目深挖模板 · 中间件 / 数据库

> 只深挖 **1 个**你做过的故事。数字全是占位符 `<...>`，答题前换成真实值。主语「我」；≥2 个数字；讲权衡；能扛 3 轮追问。

---

## 选一个讲透

| 方向 | 电梯版 |
|------|--------|
| Redis 热/大 key | 单分片打满，QPS `<X→Y>`，P99 `<A→B ms>` |
| MySQL 主从/慢 SQL/DDL | 延迟或 P99 `<X→Y>`，慢查询 `<A→B/天>`，或大表 DDL 不停写 |
| Kafka 积压 | lag `<X→0>`，根因 `<消费慢/倾斜/rebalance>`，MTTR `<T min>` |
| 缓存雪崩/命中率 | 回源 QPS `<X→Y>`，DB 未死，命中率按接口恢复 |
| 告警风暴/SLO | 夜班页数 `<X→Y>`，或错误预算驱动冻发布一次 |

不要三个浅讲。网关 502/ES 磁盘只作这个故事里的一环。

---

## STAR（2 分钟，热 key 骨架）

| | 替换占位符 |
|--|-----------|
| **S** | 集群 `<N>` 分片，日常 `<QPS>`；活动后单分片 CPU `<X%>`，接口 P99 `<A→B ms>` |
| **T** | 我 oncall：止血 + 定热点 + 防复发 |
| **A** | 定位 `<--hotkeys / 埋点 / 慢日志>` → 止血 `<L1 + 拆 key / 限流回源>` → 根因 `<单 key 无打散 + TTL 整点>` → 治理 `<规范、探测、预热、回源阈值按 DB>` |
| **R** | CPU `<X→Y%>`；P99 `<A→B>`；告警提前 `<N min>`；复发 `<0 / 有一次因为…>` |

---

## 追问必须能接到章

| 追问 | 接到 |
|------|------|
| 击穿/穿透/雪崩 | 03，不要只背定义 |
| 和 DB 谁先写 | Cache Aside 先 DB 再删；强一致读主 |
| 最左前缀 / Explain | 01 |
| 加消费者能不能消化 Lag | 05：并行度 ≤ 分区；先看下游 |
| fork 尖刺 | 02：`latest_fork_usec` |
| 这条告警该不该叫我 | 08：用户受损 + 可行动 |
| `KILL` / 重置 offset / `FLUSHDB` | 先影响面和 owner，给拒绝理由 |

---

## 数字清单（不编）

- [ ] 规模：分片/实例 `<__>`，QPS/CCU `<__>`，表数据量 `<__>`
- [ ] 故障：P99 `<__→__>`，CPU `<__%>`，lag `<__>`，回源 QPS `<__>`
- [ ] MTTR `<__ min>`
- [ ] 治理：复发、慢查询、告警提前量

说不清就诚实模糊（「大约万级 QPS」），不要假精确。
