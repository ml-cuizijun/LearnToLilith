# AI 工具实战与 Vibe-Coding

> 活动前夜，有人把 prod kubeconfig 贴进 ChatGPT 问「怎么扩容」；另有人让 Cursor Agent 一键 `kubectl apply -n prod`，diff 没看就合了——第二天限流脚本把测试路径写死成生产。
> **本章回答**：一次「用 AI 辅助排障或写代码」从提需求到合入会经历什么——人管两头、模型出草稿、验收清单挡住出域和自动执行。
> **不讲**：FC 审批闸门 → [04](../04-Function-Calling与Tool-Use/)；MCP Server 部署 → [05](../05-MCP协议/)；Agent 多步循环 → [06](../06-Agent智能体/)；GPU/推理队列 → [08](../08-GPU资源与调度/) · [09](../09-推理服务性能与稳定性/)。

**标记**：🎯重点 · 🔥难点 · ⭐常考 · 🔧实操

---

## 一、一张图：AI 辅助排障/编码的一生

> 🎯 **一句话**：AI 辅助的一生六段——人收证据、写工单、模型出草稿、人 review、测试环境验、合入或丢弃——模型永远不管签字。

```mermaid
flowchart LR
    A["人收证据<br/>指标 · 日志 · 变更"] --> B["写工单<br/>格式 · 边界 · dry-run"]
    B --> C["模型出草稿<br/>脚本 · YAML · 假设"]
    C --> D["人 review<br/>七条 checklist"]
    D --> E["测试验<br/>staging · shellcheck"]
    E --> F["合入或丢<br/>MR · 禁止 auto apply"]
    F -.->|照哪段坏了| K["查 diff · 出域 · 自动执行"]
```

**读图**：

- 主线不是「AI 替我写」，是 **人写工单、模型出中间稿、人验收**——两端必须是人。
- 「review」是最容易翻车的一段：没看 diff 就 apply，和没看化验单就签字一样。
- Vibe Coding 提效在重复劳动（骨架、样板、文档草稿），不在替你承担变更责任。

### Vibe Coding 是什么 🎯⭐

**Vibe Coding** = 用自然语言驱动 IDE/CLI 出代码草稿，**人负责需求、review、测试、合入**。不是「不用懂代码」，是「懂代码的人更快出稿」。

| 对比 | 传统写脚本 | Vibe Coding |
|------|------------|-------------|
| 起点 | 人从零敲 | 人写工单 + 模型出骨架 |
| 中间 | 人写全量 | 人迭代选中段 Cmd+K 改 |
| 终点 | 人测试合入 | **同样**人测试合入 |
| 风险 | 手误 | **+** 出域、幻觉、自动 apply |

---

## 二、人收证据：排障先有人，再要 AI 假设

> 🎯 **一句话**：排障会话开场不要「挂了怎么办」——先贴 **3~5 条证据**（指标截图、变更单、错误码），再要 **3~5 个假设 + 只读下一步命令**。

### 错误开场 vs 正确开场 🔧

```text
# 错误：闭卷 + 无边界
「大厅挂了怎么办？」→ 模型编 cordon 步骤、建议 delete pod

# 正确：开卷 + 只读
「20:05 LoginP99 500ms（Grafana 链）· 19:50 发布 gw v2.3.1 · gw-1 RestartCount=3 OOMKilled
 只要：① 3 个根因假设 ② 每条对应只读命令 ③ 不要 apply/delete」
```

模型输出应像：

```text
假设1: limit 512Mi 过低 → kubectl describe pod gw-1 -n game
假设2: v2.3.1 内存回归 → diff 发布物 + query 内存曲线
假设3: 依赖超时连坐 → ss -ti / trace 抽样
禁止: 任何 restart/scale/apply
```

> ⚠️ **别混**：RAG 查手册（03）是 **开卷**；闭卷让模型「回忆 SOP」会幻觉。AI 辅助排障 = 人证据 + 模型假设，不是 Agent 自动跑 40 步（06）。

> 📝 **面试**：排障用 AI——人先收证据，只要假设和只读命令；delete/apply 不在自动路径。

### 证据清单模板：值班可直接复制 🔧

开服夜排障，**先填表再贴给模型**——缺一项就补，不要闭卷问「挂了怎么办」。

```text
【工单模板 · 只读排障】
时间窗: 20:02–20:15
症状: LoginP99 500ms（Grafana 链: https://...）
变更: 19:50 gw v2.3.1 deploy（变更单 #8842）
已查只读证据:
  - kubectl describe pod gw-1 -n game → OOMKilled x3
  - prom: working_set 贴 512Mi limit
约束: 不要 restart/scale/apply；输出 3 假设 + 只读下一步
```

```bash
# 人侧先跑的只读命令（再贴输出给 AI）
kubectl describe pod gw-1 -n game | tail -20
kubectl top pod gw-1 -n game 2>/dev/null || echo "metrics-server 不可用"
curl -s 'http://prom/api/v1/query?query=container_memory_working_set_bytes{pod="gw-1"}' | jq '.data.result[0].value[1]'
```

**读输出**：describe 里 `Last State: OOMKilled` + prom 贴 limit = 假设 1 证据链起点；**不要**等 AI 输出后再去补 describe——顺序反了会幻觉。

| 证据类型 | 来源 | 别贴什么 |
|----------|------|----------|
| 指标 | Grafana/Prom 链 | 无链的「P99 很高」 |
| 变更 | 变更单号+时间 | 「好像发过版」 |
| Pod 状态 | describe/events | 整页 yaml 无摘要 |
| 日志 | 抽样 50 行 + 聚类 | 200MB 整文件 |

> ⚠️ **别混**：**Agent（06）自动跑 40 步** ≠ **Vibe 排障**——本章是人写工单、模型出假设；要自动只读调查走 Agent + 全护栏，不是 Cursor 里 kubectl apply。

---

## 三、写工单：Prompt 四要素 + 钉文件

> 🎯 **一句话**：工单式 prompt = **角色 + 输出格式 + 约束边界 + 验收标准**；Cursor 用 `@file` 钉已有框架，别让它从零发明目录结构。

活动前夜要 nginx 巡检脚本，工单示例：

```text
角色：SRE 巡检脚本，bash，对齐 @file scripts/check-framework.sh
输出：check-nginx.sh，函数 check_upstream / check_ssl_expiry
约束：set -euo pipefail；变量加引号；--dry-run 默认开；无硬编码密钥
验收：shellcheck 零 error；staging 跑通再 MR
```

> 💡 **打个比方**：像点外卖——「不要香菜、微辣、送到 3 楼」写清楚；厨房（模型）出菜，**你尝一口再端给客人**。

### `.cursorrules` 能挡生产变更吗？ ⭐

**不能 alone**。规则锁 **生成倾向**；真正挡执行的是：**关 Agent 自动 apply**、命令白名单、MR 必审——和 04、06 同一层。

```text
# .cursorrules 片段（生成侧）
- 生产变更只输出 diff 建议，不执行 kubectl apply
- 脚本默认 --dry-run
- 未知事实标「待核实」
```

---

## 四、模型出草稿：Cursor / Copilot / CLI 各管哪段

> 🎯 **一句话**：按 **形态** 选工具——多文件小工具 Cursor Agent；行内补全 Copilot；批量改仓 Claude Code/Codex 终端；**敏感仓不出公网**。

| 形态 | 典型工具 | 适合 | 危险品 |
|------|----------|------|--------|
| IDE 多文件 | Cursor Chat/Agent | 脚本骨架、YAML 草稿 | Agent 自动跑 shell |
| 行内补全 | Copilot | 零散函数、正则 | 无脑 Tab 接受 |
| 终端 Agent | Claude Code / Codex CLI | 批量 refactor、跑 pytest | 自动 git push |
| 对话 | 内网模型 API | 假设、文档草稿 | 贴 kubeconfig |

**Vibe 闭环**（编码）：

```mermaid
flowchart LR
    W["工单 prompt + @file"] --> G["生成草稿"]
    G --> K["Cmd+K 局部改"]
    K --> R["人 review checklist"]
    R --> T["staging 跑通"]
    T --> M["MR + 人 merge"]
```

**读图**：模型管 G→K 的中间；W 和 R→M 必须是人。Agent 弹 `kubectl apply` → **直接拒**，看 diff 手动执行。

### 内网 vs 公网：模型部署边界 🔥

| 场景 | 推荐 | 禁止 |
|------|------|------|
| 游戏服 kubeconfig、AK/SK | 内网 Copilot / 私有化 API | ChatGPT 公网对话 |
| 玩家 UID、订单号 | 脱敏后内网 | 任何外发 |
| 开源脚本骨架 | Cursor + 本地模型可选 | 贴完整 .env |
| 排障假设 | 内网 + 证据先行 | 闭卷「大厅挂了怎么办」 |

```bash
curl -s http://llm-internal/v1/models | jq '.data[].id'
grep -iE 'kubeconfig|AKIA|BEGIN RSA' /var/log/llm-audit.log && echo "SECURITY INCIDENT"
```

> ⚠️ **别混**：私有化部署 **不消除** 最小必要——GB 日志进窗口仍烧钱+漏行（10）。

### Copilot Tab：逐行验收 ⭐

行内补全适合零散函数——**安全相关行必须人眼过**：`rm -rf`、`kubectl delete`、`curl | bash`、硬编码密码。

```text
ssh $host rm -rf /data/cache/*          ← 未引号 + 破坏性
kubectl delete pod -l app=gw            ← 无 dry-run
curl https://xxx/install.sh | bash      ← 管道执行
```

> 🔍 **现场**：`git diff` 里 Copilot 行——重点看引号、路径、delete 类动词。

---

## 五、人 review：七条 checklist

> 🎯 **一句话**：生成稿合入前过 **七条**——比「看起来对」重要；看不懂的代码 **不能合**。

```text
① set -euo pipefail / 错误处理完整？
② 变量引号、路径 injection？
③ 无硬编码密钥、token、kubeconfig？
④ 幂等？重复跑会不会删两遍？
⑤ --dry-run 默认开？生产参数从 env/flag 读？
⑥ 对照官方文档/K8s API 版本——模型爱用过期写法
⑦ 未知数字/主机名标「待核实」，不编
```

Shell 示例——模型常漏 `-euo`：

```bash
# 模型首稿（不合格）
for host in $HOSTS; do ssh $host systemctl restart nginx; done

# review 后（合格）
set -euo pipefail
for host in "${HOSTS[@]}"; do
  ssh "${host}" 'systemctl is-active nginx' || echo "WARN: ${host}" >&2
done
```

> 🔍 **现场**：`shellcheck check-nginx.sh` 零 error 再 MR；`bash -n` 不够。

### 绝不能交给模型的清单 🔥

```text
✗ 完整 kubeconfig / 云 AK/SK
✗ 玩家 UID、订单号、未脱敏日志
✗ prod 上自动 apply / delete / silence P0
✗ 「你帮我直接改生产」类 Agent 任务
✗ 200MB 日志整文件进窗口——应抽样 + 聚类（10）
```

内网模型仍要 **最小必要**——出域 = 安全事件，不是「我们用的是私有部署就没事」。

### YAML / Helm 草稿：K8s API 版本陷阱 🔥

模型训练数据常含 **过期 API**——`extensions/v1beta1 Ingress`、`batch/v1beta1 CronJob` 等；review 时必须 **对照集群版本**。

```bash
# 集群支持的 API
kubectl api-resources | grep -E 'ingress|cronjob'

# server-side dry-run 验 YAML（比 client dry-run 更严）
kubectl apply --dry-run=server -f deploy-draft.yaml -n staging
# error: unable to recognize "extensions/v1beta1"  ← 模型用了过期 API

# Helm template 本地渲染
helm template mychart ./charts/mychart --namespace staging | kubectl apply --dry-run=server -f -
```

| 模型常犯错误 | 验收 |
|--------------|------|
| 过期 apiVersion | dry-run=server |
| 硬编码 namespace: prod | 改 values/env |
| resources 缺失 | 补 requests/limits |
| latest 镜像 tag | 钉 digest 或版本号 |

> 📝 **面试**：AI 出的 YAML **和手写同一验收链**——dry-run=server、MR、人 apply prod。

### Runbook 生成：人核后才能进 wiki ⭐

模型适合出 **Runbook 草稿**——时间线、命令占位、rollback 章节；**禁止**未核就进 wiki 当 SOP。

```text
【Runbook 草稿验收】
□ 每个时间点有变更单/监控链 citation，不是模型编的时间
□ 命令块经 dry-run 或 staging 验证
□ 「待补充」「待核实」标清，不伪装成已验证
□ 涉及 restart/scale 的步骤写「需变更单」，不是直接执行
```

> ⚠️ **别混**：Runbook 生成 ≠ AIOps 自动修（10）——都是 **草稿层**，execute 永远人。

---

## 六、测试验：staging 跑通再 MR

> 🎯 **一句话**：AI 出的脚本 **默认不可信**，直到在 **测试环境** 跑通 + peer review——和手写代码同一标准。

```bash
# 典型验收链
shellcheck scripts/check-nginx.sh
./scripts/check-nginx.sh --dry-run --hosts staging.txt
# OK: upstream backend OK  ← dry-run 只打印将执行的操作

# YAML 草稿
kubectl apply --dry-run=server -f deploy-draft.yaml -n staging
# 无 error 再提 MR；prod apply 人手动
```

| 阶段 | 通过标准 | 失败信号 |
|------|----------|----------|
| 静态 | shellcheck / yamllint | SC2086 未引号变量 |
| dry-run | 只打印不改 | 直接改了 staging 文件 |
| MR | 至少一人看 diff | 「AI 写的不用看」 |
| prod | 变更单 + 人执行 | Agent 自动 merge main |

> ⚠️ **别混**：**脚本确定性自动**（探针摘流、磁盘 90% 清已知目录）可以；**模型决定 restart** 不行——和 AIOps（10）同一红线。

---

## 七、合入或丢：MR 文化，禁止 auto apply

> 🎯 **一句话**：本地 commit 可以快，**push 主分支 / apply prod 必须人**；生成 Runbook 进 wiki 前人核，未知标「待补充」。

### 落地场景：活动前 nginx 巡检

1. Cursor 出 `check-nginx.sh` 骨架 → 人补 `-euo`、dry-run。
2. staging 跑通 → MR → 同事看 diff。
3. 活动日 cron 跑；**不是** Agent 活动日现场改 prod nginx。

### 落地场景：贴 kubeconfig 进公网对话

1. **立刻**轮换凭证；当安全事件。
2. 复盘：为何没走内网 Copilot；工单模板加「禁止密钥」。
3. 不是「删聊天记录就没事」。

### Cursor Agent 工作流：Cmd+L / Cmd+K / @file 🔧

> 🎯 **一句话**：Chat 出骨架，Cmd+K 改选中段，@file 钉框架——**不要**让 Agent 从零发明仓库结构。

```text
① Cmd+L：贴工单 + @scripts/check-framework.sh
② 生成 check-nginx.sh → 人发现缺 set -euo pipefail
③ 选中 for 循环 → Cmd+K：「加引号、改数组、默认 --dry-run」
④ shellcheck → staging --dry-run → MR
⑤ Agent 若弹 kubectl apply → 拒，人看 diff
```

| 操作 | 用途 | 风险 |
|------|------|------|
| Chat | 整文件骨架 | 一次接受全文件 |
| Cmd+K | 局部精确改 | 低 |
| @file | 对齐现有风格 | 不传则风格漂移 |
| Agent 跑终端 | 批量改仓 | auto apply / push |

### 排障迭代：同一会话追加证据 ⭐

第一轮只贴 P99 + 变更；模型给 3 假设。第二轮追加 `describe pod` 输出 **不要重开对话丢上下文**——但仍 **不要** auto execute：

```text
「假设 1 更像 OOM，补充：RestartCount=3 LastState=OOMKilled。
 更新假设排序；仍只要只读命令；不要 restart。」
```

> 📝 **面试**：Vibe 排障 = 人证据迭代 + 模型假设；不是 Agent 40 步自动跑（06）。

### Git + AI：commit 快，push 慢 🔧

Vibe 编码可以 **本地 commit 频繁**——但 **push main / apply prod** 必须经 MR + 人。

```mermaid
flowchart LR
    A["Cursor 出草稿"] --> B["本地 commit"]
    B --> C["MR + diff review"]
    C --> D["staging CI"]
    D --> E["人 merge"]
    E --> F["人 apply prod"]
    X["Agent auto push"] -.->|禁止| B
```

**读图**：Claude Code / Codex CLI 若配置 **auto git push** = 生产危险品——默认关；`git push --dry-run` 先看远程。

```bash
# MR 前自检
shellcheck scripts/*.sh
git diff main...HEAD --stat
git diff main...HEAD | grep -iE 'password|secret|kubeconfig|delete pod' && echo "REVIEW BLOCKER"

# 禁止 Agent 自动 push 的配置检查（Cursor/CLI 因工具而异）
# 确认: autoCommit=false, autoPush=false, terminal.autoRun=off
```

| 阶段 | AI 可帮 | 人必须 |
|------|---------|--------|
| 本地改文件 | ✓ | review diff |
| commit | ✓ 可快 | message 不含 secret |
| push / MR | ✗ 自动 | 人提 MR |
| merge main | ✗ | 至少 1 人 approve |
| prod apply | ✗ | 变更单 + 人执行 |

### 落地场景：kubeconfig 进公网对话

1. **立刻**轮换凭证；按安全事件流程。
2. 复盘：内网 Copilot + 工单模板禁密钥。
3. 不是删聊天记录就结束。

### 落地场景：活动日 nginx 巡检脚本全链路 ⭐

1. **工单**：角色 SRE bash + @file check-framework.sh + dry-run 默认。
2. **生成**：Cursor 出 `check-nginx.sh` → 人补 `set -euo pipefail`、数组引号。
3. **静态**：`shellcheck` 零 error；`bash -n` 通过。
4. **staging**：`./check-nginx.sh --dry-run --hosts staging.txt` 只打印不执行。
5. **MR**：同事看 diff，重点 upstream/ssl 检查逻辑。
6. **prod**：变更单 + cron 挂载；**不是** Agent 活动日现场改 nginx conf。

```bash
# 活动日验收：cron 跑的是 MR 合入版本，不是 Cursor 临时 buffer
md5sum /opt/scripts/check-nginx.sh
git show main:scripts/check-nginx.sh | md5sum
# 两个 hash 应一致
```

### 落地场景：模型建议 restart，人怎么走变更单

1. AI 输出「建议 restart gw-1」——**不是** Vibe 合入项，是排障假设。
2. 人确认证据链 → 走变更单 → **人** `kubectl rollout restart` 或调 limit。
3. 禁止 Cursor Agent 一键执行；禁止把 restart 写进 cron 让模型触发。

> ⚠️ **别混**：**确定性脚本**（磁盘 90% 清白名单目录）可自动；**模型建议 restart** 不行——和 AIOps（10）、Agent（06）同一红线。

---

## 八、作战：AI 编码/排障翻车怎么定责

> 🎯 **一句话**：先问 **有没有出域**，再问 **有没有未 review 就执行**，最后才问 **模型笨不笨**。

```mermaid
flowchart TB
    A["线上被 AI 改坏 / 数据泄露"] --> B{"密钥/ kubeconfig 出域?"}
    B -->|是| C["轮换凭证 · 安全事件"]
    B -->|否| D{"未 review 就 apply?"}
    D -->|是| E["流程问题 · 关 auto apply"]
    D -->|否| F{"脚本幂等/引号?"}
    F -->|否| G["review checklist 未执行"]
    F -->|是| H["正常 bug · 当人写的修"]
```

### 现象 · 先做 · 别做

| 现象 | 先做 | 别做 |
|------|------|------|
| kubeconfig 进对话 | 轮换；审计 scope | 只删聊天 |
| Agent 自动 apply prod | 关自动执行；回滚 | 「下次 prompt 写清楚点」 |
| 脚本删两遍缓存 | 查幂等；dry-run 默认 | 怪模型 |
| Runbook 时间线编造 | 人核；标待补充 | 直接进 wiki 当 SOP |
| 200MB 日志进窗口 | 抽样/聚类 | 怪 context 不够 |

### 60 秒剧本 🔧

```text
① 0–10s  是否涉及密钥/UID/完整 kubeconfig？→ 是则安全线，轮换
② 10–30s 变更是否经 MR + 人 apply？→ 否则关 Agent 自动执行
③ 30–45s 脚本 shellcheck + 是否 dry-run 默认？
④ 45–60s 定责：出域 > 未 review > 代码质量
```

---

## 九、收口

### 工具选型速查 🔧

```text
多文件脚本/YAML 草稿     → Cursor + @file 钉框架
行内补全零散代码         → Copilot（逐行看 delete/rm）
批量改仓 + 跑测试        → Claude Code / Codex CLI（禁 auto push）
排障假设 + 只读命令      → 内网对话 + 人证据先行
手册问答                 → RAG（03），不是闭卷
Runbook 草稿             → 内网 + 人核 + 标待补充
```

```bash
# 合入前一条龙
shellcheck scripts/check-nginx.sh
kubectl apply --dry-run=server -f deploy-draft.yaml -n staging
git diff main...HEAD | grep -iE 'password|secret|kubeconfig'
./scripts/check-nginx.sh --dry-run --hosts staging.txt

# 安全：secret 泄露扫描
grep -iE 'AKIA|BEGIN RSA|kubeconfig:' /var/log/llm-audit.log

# YAML API 版本
kubectl apply --dry-run=server -f deploy-draft.yaml 2>&1 | grep -i unrecognized
```

### review 七条 → 现场命令对照

| checklist 项 | 现场命令 |
|--------------|----------|
| ① -euo pipefail | `head -5 script.sh` |
| ② 引号/injection | `shellcheck -e SC2086 script.sh` |
| ③ 无硬编码密钥 | `grep -iE 'password|token|AKIA' script.sh` |
| ④ 幂等 | staging 连跑两次 diff 结果 |
| ⑤ dry-run 默认 | `./script.sh --help \| grep dry-run` |
| ⑥ API 版本 | `kubectl apply --dry-run=server -f` |
| ⑦ 待核实 | 输出含「待核实」而非编造 IP |

### 面试锚点

| 问 | 30 秒答 |
|----|---------|
| Vibe Coding 是什么？ | 自然语言出草稿，人 review+测试+合入；提效重复劳动，不替值班签字。 |
| Cursor/Copilot/CLI 怎么选？ | 按形态：多文件 Cursor，补全 Copilot，批量改仓终端 Agent；敏感不出公网。 |
| 运维脚本注意什么？ | checklist 七条；dry-run 默认；staging 跑通；对照官方文档。 |
| 什么绝不能给模型？ | kubeconfig、UID、未脱敏日志、auto apply prod。 |
| 排障怎么用 AI？ | 人先证据；3~5 假设+只读命令；同会话迭代；不 auto delete。 |
| .cursorrules 够吗？ | 不够；关 auto apply + 白名单 + MR。 |

### 易错一句话对照

- Vibe Coding 不用懂代码 → 人必须 review，看不懂不能合
- 内网模型可以贴 kubeconfig → 最小必要，出域仍要轮换
- Agent 自动 apply 省事 → 默认禁止；人看 diff
- AI 写的脚本不用 shellcheck → 和手写同一标准
- 闭卷问「挂了怎么办」 → 先贴证据再要假设
- Runbook 生成完直接进 wiki → 人核，未知标待补充
- 200MB 日志丢窗口 → 抽样；聚类后给 LLM（10）
- Copilot Tab 全接受 → 逐行看，尤其安全相关
- dry-run 可选 → 默认开
- 模型建议 restart → 人走变更单，不是 Vibe 合入
- 内网模型可贴全量日志 → 仍抽样聚类
- Copilot Tab 全接受 → 逐行看 delete/rm
- helm template 不验 → dry-run=server
- auto git push 省事 → 默认禁止

### 数字墙

> review **7** 条 checklist · 排障开场证据 **3~5** 条 · 假设 **3~5** 个 · 日志进窗口 **抽样** 不整文件 · MR **≥1** 人看 diff · prod apply **0** 次 Agent 自动 · shellcheck **0** error 再合入 · dry-run=**server** 验 YAML · autoPush **关**

---

## 合上书

- [ ] **默画主图**：人收证据 → 写工单 → 出草稿 → review → 测试 → 合入/丢。
- [ ] **口述 Vibe Coding**：和「AI 替我写」差在哪？谁管两头？
- [ ] **口述排障开场**：错误问法 vs 正确问法；输出应长什么样？
- [ ] **口述工单四要素**：角色、格式、约束、验收；@file 干什么？
- [ ] **口述 review 七条**：逐条说为什么；模型常漏什么？
- [ ] **口述工具选型**：Cursor/Copilot/CLI 各适合什么？危险品是什么？
- [ ] **口述绝不能给的清单**：kubeconfig 出域后第一步做什么？
- [ ] **口述 staging 验收链**：dry-run、shellcheck、MR 顺序。
- [ ] **口述与 Agent/AIOps 边界**：自动 apply vs 确定性脚本自动。
- [ ] **定责**：出域 / 未 review / 幂等 bug 各先查什么？

下一章：[08 GPU 资源与调度](../08-GPU资源与调度/)（只管资源，不管 TTFT 服务质量细节）。

### 附录：Vibe 排障 vs Agent vs AIOps 选型对照

| 场景 | 用啥 | 为什么 |
|------|------|--------|
| 人要 3 假设+只读命令 | Vibe（07） | 人写工单，模型出草稿 |
| 多源只读自动拉证据 | Agent（06） | ReAct+护栏 |
| 80 条告警摘要 | AIOps（10） | 规则先行+LLM JSON |
| 自动 restart | **都不行** | 人+变更单 |

> 📝 **面试**：三者在 **execute 层** 同一红线——模型不签字。

### 合上书补充

- [ ] **证据清单模板**：排障工单四块填什么？
- [ ] **dry-run=server** 与 client dry-run 差在哪？
- [ ] **Vibe vs Agent vs AIOps** 选型表口述。
- [ ] **cron 脚本 hash** 与 git main 对齐为什么重要？

> 数字墙补充：证据清单 **4** 块 · YAML 验 **server dry-run** · 出域 **0** 容忍
