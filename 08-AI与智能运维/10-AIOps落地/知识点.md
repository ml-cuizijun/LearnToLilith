# AIOps 落地

> 开服 20:00，飞书群 80 条告警刷屏：登录超时、gw-1 OOM、推理 TTFT 8s、支付 P99 飙红全混在一起。有人想让模型「自动回滚大厅」；另有人只报「降噪 80%」——支付 P0 被合成进「登录抖动」没人看见。
> **本章回答**：一次 **AIOps 辅助排障** 从告警风暴到给人可行动假设会经历什么——规则降噪、LLM 摘要、根因假设、**禁止自动修**。
> **不讲**：Prometheus/Alertmanager 基础 → [09-02](../../09-可观测性与SRE方法论/02-Prometheus与PromQL/)；通用告警降噪方法论 → [09-03](../../09-可观测性与SRE方法论/03-告警设计与降噪/)；Agent ReAct → [06](../06-Agent智能体/)。

**标记**：🎯重点 · 🔥难点 · ⭐常考 · 🔧实操

### 与 06 Agent、07 Vibe、09-03 的分工

| 能力 | 本章 | 其他章 |
|------|------|--------|
| 告警降噪规则 | 规则层先行 | 通用 [09-03](../../09-可观测性与SRE方法论/03-告警设计与降噪/) |
| LLM 摘要 JSON | ✓ | — |
| 只读多步拉证据 | — | [06 Agent](../06-Agent智能体/) |
| 人写工单出假设 | — | [07 Vibe](../07-AI工具实战与Vibe-Coding/) |
| 推理 queue/TTFT | — | [09 推理](../09-推理服务性能与稳定性/) |
| auto-fix | **禁止** | 06/07/09 同红线 |

> 🎯 **一句话**：AIOps 管 **告警理解层**；execute 层与 Agent/Vibe **同一红线**——模型不签字。

---

## 一、一张图：AIOps 辅助排障的一生

> 🎯 **一句话**：AIOps 的一生六段——原始告警、规则降噪、同根因聚类、LLM 摘要、根因假设、**人决策**——模型停在建议，不碰 execute。

```mermaid
flowchart LR
    A["原始告警<br/>Prom/AM firing"] --> B["规则层<br/>fingerprint · 维护窗"]
    B --> C["聚类<br/>同根因合并"]
    C --> D["LLM 摘要<br/>低温 JSON"]
    D --> E["根因假设<br/>证据 · 非定论"]
    E --> F["人决策<br/>禁止 auto-fix"]
    F -.->|照哪段坏了| K["漏 P0 · 自动修 · 幻觉"]
```

**读图**：

- 主线在 **监控之上** 的理解层——Prometheus/Alertmanager 仍是 **真相源**；AIOps 不替代采集、不替代 oncall 签字。
- 「规则层」必须先于 LLM——80 条进模型是烧钱+漏报；P0 原通道 **必须仍响**。
- 最后一节永远是 **人**：silence/rollback/restart **不接模型**。

### AIOps 不是什么 🎯⭐

| 是 | 不是 |
|----|------|
| 对已采集信号做降噪、摘要、假设 | 新监控系统 |
| 嵌进 IM/工单的 **草稿** | 替值班大脑 |
| 辅助理解「可能同一根因」 | 自动执行变更 |
| 复盘/RCA **起草**（人核） | 编造时间点进 wiki |

> 📝 **面试**：AIOps = 监控之上的理解层；真相源不变；**不能 auto-fix 生产**。

---

## 二、原始告警：真相源不变

> 🎯 **一句话**：`LoginP99High`、`GwOOM`、`InferenceTTFT` 各走 **原 Alertmanager 规则通道**——AIOps 是 **附加摘要**，不是拦截 P0。

开服夜典型 firing 列表：

```text
[ firing ] LoginP99High        severity=P1  fingerprint=login_timeout
[ firing ] GwOOMKilled         severity=P1  pod=gw-1
[ firing ] InferenceTTFTHigh   severity=P2  route=rag
[ firing ] PayP99High          severity=P0  ← 绝不能被摘要层吃掉
```

> ⚠️ **别混**：**确定性阈值**（磁盘 90%、探针失败摘流）仍用脚本/控制器——贴「AI 自愈」标签但其实是 if 判断，不算 AIOps 模型决策。

> 💡 **打个比方**：血压计（Prometheus）测的是原始数据；主治医生读报告写「可能三高，建议复查」是理解层——**医生不能替代血压计，也不能不经你同意就开刀**。

---

## 三、规则层降噪：fingerprint、维护窗、severity 闸门

> 🎯 **一句话**：**规则层先行**——同 fingerprint 合并、维护窗压已知噪音、P3 不进 LLM；模型只处理 **收束后的十几条**。

```text
80 条原始 firing
  → fingerprint 合并（同 login_timeout 47 条 → 1 条）
  → 维护窗（CDN 预热已知 → suppress）
  → severity 闸门（P3 仅工单，不进群摘要）
  → 剩余 ~12 条进 LLM 摘要
```

```bash
# Alertmanager 侧（规则层，非 LLM）
amtool alert query 'alertname=LoginP99High' | wc -l
# 47  ← 同根因重复

# 合并后送 LLM 的 payload 应 <20 条，且含 severity/P0 标记
```

| 规则手段 | 作用 | 失败信号 |
|----------|------|----------|
| **fingerprint** | 同标签重复告警合并 | 47 条独立 Login |
| **维护窗** | 已知变更期 suppress | 预热 CDN 仍刷屏 |
| **severity 路由** | P0 独立通道 | P0 只出现在 LLM 散文里 |
| **inhibit** | 上游根因抑制下游 | OOM 还刷 HPA 子告警 |

> 📝 **面试**：降噪 **规则层先行**，LLM 收束；三率：**条数、有效率、漏报**——只报「降噪 80%」不够，**漏 P0/P1 = 失败**。

通用方法论详见 [09-03 告警设计与降噪](../../09-可观测性与SRE方法论/03-告警设计与降噪/)——本章只讲 **AI 能帮到哪、哪一步必须人**。

### inhibit 与依赖链：规则层示例 🔧

```text
# 示意：GwOOMKilled firing 时抑制下游 LoginP99 重复刷屏
inhibit_rules:
  - source_match: { alertname: GwOOMKilled }
    target_match: { alertname: LoginP99High }
    equal: ['cluster']
```

```bash
# 验证 inhibit 是否生效
amtool alert query 'alertname=LoginP99High' --output json | jq '.[] | .status.inhibitedBy'
# 非空 ← 被上游 OOM 抑制；若仍 47 条独立 Login → inhibit 未配
```

**读输出**：inhibit 是 **规则层**，不是 LLM「聪明合并」——LLM 之前必须先压到 ~12 条。

> ⚠️ **别混**：inhibit **漏配** 不能靠 prompt「请合并重复告警」——模型可能误合并 P0。

---

## 四、聚类与 LLM 摘要：同根因一条话

> 🎯 **一句话**：聚类把 **可能同一事故** 的告警放一组；LLM 用 **低温 JSON + schema** 输出「一条群消息草稿」——失败 **降级规则层**，不推半截散文。

送 LLM 的输入结构（示意）：

```json
{
  "alerts": [
    {"name": "LoginP99High", "sev": "P1", "since": "20:02", "labels": {"svc": "login"}},
    {"name": "GwOOMKilled", "sev": "P1", "pod": "gw-1"},
    {"name": "PayP99High", "sev": "P0", "since": "20:03"}
  ],
  "recent_deploy": [{"svc": "gw", "ver": "v2.3.1", "at": "19:50"}]
}
```

期望输出（schema 约束）：

```json
{
  "summary": "20:02 起登录 P99 升高，gw-1 OOM；支付 P0 独立并列，需立即 ack",
  "groups": [
    {"theme": "login_gw", "alerts": ["LoginP99High", "GwOOMKilled"], "hypothesis": "gw 内存/regression"},
    {"theme": "pay", "alerts": ["PayP99High"], "hypothesis": "待查，不与 login 合并"}
  ],
  "p0_ack_required": ["PayP99High"],
  "auto_action": "none"
}
```

**读输出**：`PayP99High` **独立成组**且 `p0_ack_required`——不能把 P0 合成进「登录抖动」。`auto_action` 永远是 **none**。

> ⚠️ **别混**：LLM 摘要 **人审后进群**（或 bot 发草稿 @oncall 确认）；不是 auto silence。半截 JSON 失败 → **只推规则层合并结果**，不硬凑散文。

### LLM 参数：低温 + schema 约束 🔧

摘要不是创意写作——**temperature 低（~0~0.2）** + **JSON schema 强制**，减少散文和漏字段。

```text
temperature: 0.1
response_format: json_schema
required_fields: [summary, groups, p0_ack_required, auto_action]
```

| 参数 | 生产建议 | 失败现象 |
|------|----------|----------|
| temperature | 0~0.2 | 高则编造 deploy 时间 |
| max_tokens | 够容纳 schema | 截断丢 p0 字段 |
| schema | 硬校验 | 半截 JSON → 降级规则层 |

```bash
# 出站 JSON schema 校验（CI 或 bot 侧）
jq -e '.p0_ack_required and .auto_action=="none"' /var/log/aiops/outbound-last.json
# 校验失败 → 不群发，只推 amtool 合并结果
```

> 📝 **面试**：摘要失败 **降级规则层**——不是 retry 10 次让模型「认真点」。

---

## 五、根因假设：证据链，不是定论

> 🎯 **一句话**：根因分析 AI 只输出 **假设 + 证据**（变更链接、指标截图、RAG 案例 citation）——**不说「根因已确定」**，不定责，不 auto rollback。

```text
假设1（中）: gw v2.3.1 内存回归
  证据: 19:50 deploy · gw-1 OOMKilled · 内存曲线贴 limit
  下一步（只读）: describe pod · query 内存 · diff 发布物

假设2（低）: 登录链路上游 DNS
  证据: 不足，需 dig 四路（15）
  下一步: dig login.game.com @各递归

禁止: 「根因已确定为 DNS」· 自动 rollback gw
```

RAG（03）挂 **相似复盘 citation**——开卷，不闭卷编 cordon 步骤。

| 输出 | 允许 | 禁止 |
|------|------|------|
| 假设排序 | ✓ | 「已确定」 |
| 变更/API 链接 | ✓ | 自动 apply |
| RAG citation | ✓ | 过期 SOP 不核就执行 |
| 只读下一步 | ✓ | restart/scale/silence P0 |

Agent（06）可 **代劳只读工具** 拉证据——仍 **无写工具**；AIOps 产品层 **禁止 auto-fix** 是硬边界。

### 变更关联：摘要输入必须带 deploy 上下文 ⭐

开服夜 **19:50 gw v2.3.1** 与 **20:02 Login P99** 关联——靠 **结构化 recent_deploy**，不是模型闭卷猜。

```json
"recent_deploy": [
  {"svc": "gw", "ver": "v2.3.1", "at": "19:50", "change_id": "8842"}
]
```

```bash
# 人侧也可先查再决定是否送 LLM
kubectl rollout history deployment/gw -n game --revision=3
# 或变更系统 API
curl -s http://change-api/v1/recent?window=1h | jq '.[] | select(.svc=="gw")'
```

**读输出**：无 deploy 上下文时，LLM 假设「内存 regression」置信度应标 **low**——schema 里要有 `confidence` 字段。

| 证据 | 来源 | 禁止 |
|------|------|------|
| 变更时间 | 变更单 API | 模型编「大概 8 点发版」 |
| OOM | Prometheus/describe | 闭卷 |
| 相似复盘 | RAG citation | 无 cite 的 SOP |

### 收束：AIOps 落地三原则

```mermaid
flowchart TB
    subgraph three ["AIOps 三原则"]
        N["降噪 · 规则先行 · 不漏 P0"]
        H["假设 · 证据 · 非定论"]
        X["禁止 · auto-fix · auto silence P0"]
    end
```

**读图**：缺任一原则，成熟度应停在 **建议型**——不能因「有模型」就升自动修。

> 🔥 **难点**：**脚本确定性自动**（磁盘 90% 清已知目录、探针摘流）可以；**模型决定 restart** 不行——面试必答。

---

## 六、人决策：禁止自动修、禁止 auto silence P0

> 🎯 **一句话**：**silence / rollback / restart 有状态服 / scale / delete vllm** 永远 **人审**——模型最多生成 **审批单 JSON**，执行器不接 LLM 输出。

```text
✗ 模型 auto silence P0/P1
✗ 模型 auto rollback 大厅
✗ 模型 restart 有状态 gw
✗ 模型 scale 推理 Deployment
✓ 人 ack P0 后走变更单
✓ 确定性脚本：磁盘 90% + 白名单路径清理（非 LLM 决策）
✓ Agent 只读调查 + 提议（06）
```

| 成熟度 | 做什么 | 不做什么 |
|--------|--------|----------|
| **L1 建议型**（默认） | 摘要、假设、只读命令建议 | 任何 execute |
| **L2 半自动** | 人点按钮触发 **预审计脚本** | LLM 直接触发 |
| **L3 自动修** | **游戏有状态生产禁止** | — |

> 📝 **面试**：为什么 AI 不能自动修？幻觉 + 有状态一次错杀 = 事故；写路径不接模型。和 Agent 不能替 Oncall（06）同一红线。

### IM 嵌入：摘要进飞书/Slack 的接线 🎯⭐

AIOps 价值在 **值班群能直接看到**——独立 Web 控制台没人打开 = 零价值。

```mermaid
flowchart LR
    AM["Alertmanager"] --> R["规则层合并"]
    R --> L["LLM 低温 JSON"]
    L --> B["Bot 草稿"]
    B --> H["人 @oncall 确认"]
    H --> G["群发摘要"]
    X["auto silence"] -.->|禁止| AM
```

**读图**：

- Bot 发的是 **草稿**——`p0_ack_required` 字段必须 @ 对应 Oncall；不是 auto ack。
- 半截 JSON 解析失败 → **只推规则层合并结果**，不硬凑散文进群。
- 群里「已自动 rollback」文案 = **事故**，查 execute 层有无 LLM 接线。

```bash
# 验收 bot 出站 payload（示例）
jq '{summary, p0_ack_required, auto_action}' /var/log/aiops/outbound-last.json
# auto_action 必须 "none"
```

> ⚠️ **别混**：**飞书卡片按钮触发预审计脚本（L2）** ≠ **LLM 直接 execute**——按钮背后仍是人点 + 固定脚本。

---

## 七、观测与迭代：三率对照表

> 🎯 **一句话**：活动周用 **对照表** 评 AIOps——**条数、有效率、漏报**；只报「降噪 80%」不证明没漏 P0。

```text
| 指标 | 活动前基线 | 活动周 | 目标 |
|------|------------|--------|------|
| 群告警条数/小时 | 120 | 28 | 降 ≠ 唯一 KPI |
| 摘要有效率（人标有用） | - | 85% | >80% |
| P0/P1 漏报 | 0 | 0 | 必须 0 |
| 误合并（P0 进错组） | - | 0 | 必须 0 |
```

日志 **聚类** 后抽样给 LLM——GB 整文件进窗口是烧钱+漏关键行（07）。

### 成熟度模型：默认停在 L1 建议型 ⭐

```text
L0 观测    只有 Prom/AM，无 AI
L1 建议型  摘要+假设+只读建议 ← 生产默认停这里
L2 半自动  人点按钮触发预审计脚本（非 LLM 触发）
L3 自动修   游戏有状态生产禁止
```

| 级别 | LLM 能做什么 | execute 层 |
|------|--------------|------------|
| L1 | JSON 摘要、假设排序 | **无** LLM 接线 |
| L2 | 同上 | 人确认→固定脚本 |
| L3 | — | **不接** |

> 📝 **面试**：没落地也讲清 **为什么不能到 L3**——幻觉 + 有状态错杀。

### 日志聚类：GB 日志不能整进窗口 🔧

```text
原始 ERROR 200 万行
  → 规则/指纹聚类 → 847 类
  → 每类抽 3~5 行代表
  → 共 ~4000 行送 LLM 做「可能同一发布导致」假设
  → 输出仍带 citation 到原始 log_id
```

> ⚠️ **别混**：聚类是 **降采样**；不是让模型「读全量日志」。整文件进窗口 = 烧钱 + 漏关键行（07）。

### 异常检测 vs 阈值：AIOps 补位，不替代 🔥

**异常检测**（ML 说「今天不像往常」）补 **动态基线**——**不替代** 磁盘 90%、探针失败等 **硬阈值**。

| 手段 | 管什么 | AI 角色 |
|------|--------|---------|
| 硬阈值 | 磁盘满、5xx 率 | 规则，非 LLM |
| 异常检测 | 流量形态突变 | 可 ML，摘要给人 |
| LLM 摘要 | 12 条告警理解 | 低温 JSON |
| 根因假设 | 排序+证据 | 非定论 |

```text
PayP99High P0 → 必须硬阈值 firing，不能等「异常检测觉得怪」才响
Login 流量 anomaly → 可 ML 标注，送 LLM 时附「anomaly_score=0.92」
```

> 📝 **面试**：异常检测 **不替代监控**；P0 硬阈值 + 原 AM 通道必须保留。

### 反馈闭环：摘要质量怎么迭代 ⭐

活动周三率对照后，**人标「摘要有用/无用」** 进反馈表——调 prompt/schema，不是加 auto-fix。

```text
| session_id | useful | reason |
| oncall-88  | yes    | P0 独立清晰 |
| oncall-89  | no     | 误合并 pay 到 login |
```

> 🔍 **现场**：误合并率 >0 → **关 LLM 合并 P0**，不是「换更大模型」。

### 落地场景：没 AIOps 平台怎么答面试

「若从零，登录链路三层：① AM fingerprint 合并 ② 维护窗压 CDN 预热 ③ 低温 JSON 摘要进飞书 **人审后发**；P0 支付独立 ack；execute 零 LLM；活动周三率对照。」——不编「我们有个 AI 大脑平台」。

---

## 八、作战：AIOps 翻车怎么定责

> 🎯 **一句话**：先问 **P0 是否仍独立响**；再问 **有没有 auto-fix**；最后问 **摘要是否幻觉**。

```mermaid
flowchart TB
    A["AIOps 翻车"] --> B{"P0 仍独立 ack?"}
    B -->|否| C["合成/漏报 · 关 LLM 摘要"]
    B -->|是| D{"有 auto-fix?"}
    D -->|是| E["事故 · 下线 execute"]
    D -->|否| F{"摘要与 firing 一致?"}
    F -->|否| G["幻觉 · 降级规则层"]
    F -->|是| H["人判断失误 · 非 AI 独责"]
```

### 现象 · 先做 · 别做

| 现象 | 先做 | 别做 |
|------|------|------|
| P0 被合成进 P2 摘要 | 关 LLM 合并 P0；规则隔离 | 调 prompt「认真点」 |
| 模型 auto silence | 下线写接口；审计 | 「下次加确认」 |
| 根因「已确定」文案 | 改 schema 禁措辞 | 当最终 RCA |
| 降噪 80% 但漏支付 | 以漏报为准判失败 | 只报降条数 |
| GB 日志进模型 | 聚类+抽样 | 怪 context 小 |

### 60 秒剧本 🔧

```text
① 0–10s  P0 firing 与原 AM 通道是否仍在？独立 ack 了吗？
② 10–30s  摘要 JSON 里 p0_ack_required 是否完整？
③ 30–45s  执行层有无 LLM 触发的 silence/rollback？
④ 45–60s  漏报→关 AI 合并；auto-fix→事故；幻觉→降级规则层
```

### 开服夜完整剧本：80 条 → 12 条 → 1 条群消息 ⭐

```text
T+0min   80 firing：Login×47 GwOOM InferenceTTFT PayP0
T+2min   规则层：fingerprint Login→1 · PayP0 独立 ack
T+5min   12 条送 LLM（含 recent_deploy gw v2.3.1）
T+6min   JSON：login_gw 一组 · pay 独立 · auto_action=none
T+7min   人审 → 群发 · @pay-oncall ack P0
T+8min   Agent（06）只读拉 gw 证据
T+30min  人变更单调 limit——execute 无 LLM
```

> 🔍 **现场**：复盘查 `auto_action` 历史——任何非 none = 事故线索。

---

## 九、收口

### 落地 checklist 🔧

```text
□ P0 原规则通道仍响，LLM 只附加
□ 规则 fingerprint/维护窗/inhibit 先于 LLM
□ LLM 低温 JSON + schema；失败降级规则层
□ 根因输出「假设+证据」，禁「已确定」
□ execute 层无 LLM 接线
□ 活动周三率对照表：条数/有效率/漏报
□ 嵌 IM/工单，不做没人打开的 APP
□ recent_deploy 结构化进摘要输入
□ auto_action 字段恒为 none
```

```bash
# 规则层：firing 条数
amtool alert query | wc -l
amtool alert query 'severity=critical'   # P0 必须可见

# LLM 出站验收
jq '{p0_ack_required, auto_action, groups[].theme}' /var/log/aiops/summary-last.json

# execute 层审计：有无 LLM 触发变更
grep -iE 'llm_trigger|auto_silence|auto_rollback' /var/log/change-audit.log

# 日志聚类后抽样规模
wc -l /tmp/log-cluster-sample.txt   # 应 ~4000 行级，非 200 万
```

### 面试锚点

| 问 | 30 秒答 |
|----|---------|
| AIOps 是什么？ | 监控之上理解层；降噪、摘要、假设；不替代 Prom/AM。 |
| 降噪怎么做？ | 规则层先行 fingerprint/维护窗；再 LLM 收束；漏 P0=失败。 |
| 为何不能自动修？ | 幻觉；有状态错杀=事故；写路径不接模型。 |
| 根因 AI 怎么做？ | 假设+证据+只读下一步；不当定论；不定责。 |
| 没落地项目怎么答？ | 讲边界+若从零：登录链降噪三层、只读、可度量。 |
| 异常检测替代监控吗？ | 不替代；补「今天不像往常」；硬阈值仍要。 |
| 与 Agent 关系？ | Agent 只读拉证据；AIOps 禁止 auto-fix。 |
| 与 09-03 关系？ | 通用降噪在 09-03；本章 AI 边界。 |

### 易错一句话对照

- AIOps 替代监控 → 理解层，真相源不变
- 80 条全进 LLM → 规则层先行
- 降噪 80% 就够 → 漏 P0/P1=失败
- P0 写进摘要就行 → 必须独立 ack 通道
- 根因已确定 → 只能是假设
- auto silence 省事 → 禁止 P0/P1
- 模型 auto rollback → 永远人审
- 脚本摘流 + AI 标签 → 确定性可以，模型决策不行
- GB 日志给 LLM → 聚类抽样
- 没项目编平台 → 讲边界+窄场景 hypothetical
- Agent 自动修 → 06 也禁止；10 产品层再强调
- inhibit 靠 LLM → 规则层 inhibit 先行
- 异常检测替代 P0 阈值 → 不替代
- IM 按钮=L3 自动修 → L2 人点+固定脚本
- 无 recent_deploy 高置信度假设 → schema 标 low

### 数字墙

> 原始告警 **80** 条 → 规则层 **~12** 条进 LLM · 摘要 **低温 JSON** · **P0 漏报 = 0** 才合格 · 成熟度默认 **L1 建议型** · 三率：**条数 / 有效率 / 漏报** · execute 层 LLM 接线 **0**

---

## 合上书

- [ ] **默画主图**：原始 → 规则降噪 → 聚类 → LLM 摘要 → 假设 → 人决策。
- [ ] **口述 AIOps vs 监控**：真相源是谁？理解层做什么？
- [ ] **口述规则层四手段**：fingerprint、维护窗、severity、inhibit。
- [ ] **口述 LLM 摘要 schema**：P0 为什么要独立字段？auto_action 是什么？
- [ ] **口述根因假设**：允许什么、禁止什么措辞？
- [ ] **口述禁止自动修清单**：silence/rollback/restart 为什么不行？
- [ ] **口述脚本自动 vs 模型自动**：磁盘 90% 清理为什么可以？
- [ ] **口述三率对照表**：为什么只报降噪 80% 不够？
- [ ] **口述与 06 Agent、09-03 分工**。
- [ ] **定责**：P0 漏报 / auto-fix / 摘要幻觉各怎么处理？

### 附录：RCA 起草 vs 根因定论

活动后 **RCA 文档** 可由 LLM **起草**——人核时间线、定责、行动项；和本章「根因假设」同一边界。

```text
LLM 可出: 时间线草稿 · 可能因素列表 · 待办 checkbox
LLM 不可: 「根因已确定为 X」· 处分建议 · auto 行动项 execute
人必须: 每个时间点 citation · 最终定责签字 · 行动项 owner
```

```bash
# RCA 草稿验收
grep -c '待核实\|citation' rca-draft.md
grep -i '根因已确定' rca-draft.md && echo "REJECT DRAFT"
```

### 合上书补充

- [ ] **inhibit 规则** 与 LLM 合并分工。
- [ ] **LLM temperature/schema** 生产参数。
- [ ] **开服夜 80→12→1 剧本** 逐步 execute 边界。
- [ ] **异常检测 vs P0 硬阈值** 为什么都要？

> 数字墙补充：进 LLM 告警 **~12** 条 · temperature **0~0.2** · auto_action 恒 **none**

### 10 一句话收口

**规则先行降噪，LLM 只出 JSON 草稿，假设非定论，execute 零 LLM——漏 P0 即失败。**

上一章：[09 推理服务](../09-推理服务性能与稳定性/)。告警方法论：[09-03 告警设计与降噪](../../09-可观测性与SRE方法论/03-告警设计与降噪/)。
