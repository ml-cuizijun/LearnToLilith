# 全库改写标准

**样板章（结构 + 讲解风格）：**

| 类型 | 样板章 | 风格 |
|------|--------|------|
| **默认详度 · 讲解套路（全库）** | `01-基础底座/12-网络栈与Socket/知识点.md` | **总图 → 走读 → 名词 → 对象/状态 → 易混专节 → 定责**；难概念「一例钉死」 |
| 分层排障 | `01-基础底座/04-内核与系统/知识点.md` | 主线 + 命令输出（已按 12 章样板标尺加厚） |
| K8s 部署 | `03-Kubernetes/02-etcd/知识点.md` | 安装部署 + 排障 |

根 `README.md`「规范」一节同样有效。

---

## 讲解套路（后续章按此写 · 12/原06 沉淀）

> 目标：能**默画总图**、能**对着 ss/监控定责**、难概念**一遍听懂**。不追求百科全，不在多节重复同一 FAQ。

### 一章怎么排

| 顺序 | 写什么 | 要点 |
|------|--------|------|
| 0 | 开篇 | 解决什么 / 读法 / **本章不讲**（链出去） |
| 1 | **总图先行** | 一张大坐标系；读图 2～3 句；主图只留一套（ASCII 或 mermaid 二选一为主） |
| 2 | **完整走读（可选）** | 用现网角色串一遍；**只保留一版**（单线程模型即可） |
| 3 | 名词扫盲 | 对照总图；易懂点到为止 |
| 4 | 核心对象与状态 | 先定义再使用；命令怎么认 |
| 5 | **最易混专节** | 全章最易晕的一点单独钉死 |
| 6 | 机制深化 | 队列 / 超时 / 满了会怎样 |
| 7 | 定责树 + 面试 + 合上书 | 现象\|先做\|别做 |

### 单个难概念怎么讲（固定四步）

```text
① 最常见具体例子（先客户端/服务端，再抽象「本端」）
② 一句话定义
③ 对照命令怎么认（ss / mpstat 长什么样）
④ 易错一句钉死（只写一次；别处「见 §x」）
```

### 写法要 / 不要

| 要 | 不要 |
|----|------|
| FAQ 归入首次出现的节 | 同一点在 2.5 / 2.6 / 4 / 6 各讲一遍 |
| 链到主责章（03/08/11） | 为全面把别章细节塞进来 |
| 验收：默画总图 + 定责树 | 合上书题对不上正文 |

**密度参考**：以 `12-网络栈与Socket` 为详度标尺（约 1000～1600+ 行，讲透为止、禁止注水）；邻章用链接，不重复 FAQ。

---

## 知识点.md：固定结构

每章顶部 mermaid 导航（按主题微调第 4 步名称）：

```mermaid
flowchart LR
    a[1 概念] --> b[2 架构]
    b --> c[3 原理]
    c --> d[4 落地/部署]
    d --> e[5 配置]
    e --> f[6 命令与操作]
    f --> g[7 排障]
    g --> h[8 常考]
```

### 开篇（固定四块）

1. **事故/场景一句** — 这章解决什么生产问题
2. **本章主线（3 步）** — 读者排障/学习的主线，编号 1. 2. 3.
3. **读完你能** — 3～5 条可检验能力
4. **本章目录** — 只列一级/二级，不写「打开大纲」等 meta 说明

然后：**本章不讲**（链到其它章）→ `---` → 正文。

### 正文目录（顺序固定，不适用可整节省略）

| # | 节名 | 写法 |
|---|------|------|
| 1 | 基本概念 | 表格定义；> **记住** 一句 |
| 2 | 架构图 | mermaid + **读图 N 句话** |
| 3 | 原理 | 每小节开头 **一句话** + 图/表 + 必要时命令示例与输出 |
| 4 | 落地 / 安装部署 | 生产步骤、选型；K8s 章用「安装部署」 |
| 5 | 核心配置 | 关键配置项 + 示例 |
| 6 | 命令与操作 | 合并原「命令」「操作」；速查表 + 典型输出 |
| 7 | 生产排障 | 现象 \| 先做 \| 别做 |
| 8 | 必会 · 常考 | 对错表 + 数字 + 易混 |
| — | 合上书，问自己 | 5～7 道自测题 |

**开发能力（06）章** — **不用** 9 步 mermaid 导航。按「面试 + 语法 + 原理 + 模板」：

| 节 | 内容 |
|----|------|
| 开篇 | 本章目标一句 + **面试怎么考** 表（核心/必会/常考）+ **30 秒怎么说** |
| 能力边界 | Shell/Python/Go/正则 各干什么、不干什么 |
| **语言最小集** | 读写脚本/认代码的**最小语法**（变量、循环、函数、err 处理）；不是语言大全 |
| 语法必会 | 按 SRE 场景组织：用法 + 示例 + 易错（不是语法大全） |
| 原理讲透 | 每节 **一句话** + 图；讲清「为什么这样写」 |
| 生产模板 | **完整可复制** 脚本/程序 + 典型输出 |
| 踩坑清单 | 现象 \| 原因 \| 修法 |
| 必会 · 常考 | 对错表 + 易混 |
| 合上书 | 5～6 自测 |

**AI 与智能运维（09）章** — **不用** 9 步 ops 导航。按「面试 + 边界 + 原理 + 口述」：

| 节 | 内容 |
|----|------|
| 开篇 | 加分项定位 + **面试怎么考**（问法 \| 30 秒 \| 2 分钟） |
| SRE 边界 | 该用 / 禁止（出域、自动执行、替值班） |
| 原理讲透 | 图 + 白话；拒绝论文体 |
| 落地场景 | 1～2 个值班/平台真实例子 |
| **口述模板** | 【30 秒】【2 分钟】【常见追问】— 可直接背 |
| 必会 · 常考 | 对错 + 易混 |
| 合上书 | 5～6 自测 |

> 06、09 的 `练习题.md`：口述题 + 边界判断 + 读代码/场景设计；仍保留 **对应** + **一句话**。

### 教材骨架 + 作战肌肉（默认 · 全库推荐）

**读者假设**：有经验但知识散——缺的是**位置感与脉络**，不是幼儿园式手把手。

**目标**：合上书能**默画全章架构**；给故障能**指到决策树分支**；面试锚点能口述。教材骨架（地图+概念+机制）+ 作战肌肉（定责+命令+参数）。

**固定结构：**

```markdown
# 章节名
> 这章解决什么 + 读法 + 验收两件事（默画架构 / 故障定责）

**标记**：🎯重点 · 🔥难点 · ⭐常考 · 🔧实操

## §0 你在哪（地图入口 · 按主题选 1～3 张）
- 整机位置 / 分层归属 / 请求或包旅行（网络章三张都要）
- 相邻概念「三句话点到为止」+ 链到主责章（不膨胀本章）
- 本章主线 + 读完你能 + 本章不讲

## §1 基础扫盲（只建本章感）
- 核心对比表、必会名词、1 张本章内数据流图
- 不把别章地基展开（地图+链接，见设计合同）

## §2 本章架构图（合上书默画用）
- master mermaid + 读图 N 句

## §3 核心知识详解
- 机制图优先；图旁 2～3 句人话（少长篇文字比喻）
- 🎯🔥⭐ 打在考点上；关键数字；定责/决策表；易混 1 行

## §4 作战：排障与落地
- 定责决策树 · 现象|先做|别做 · 选型 · 命令+输出解读

## §5 参数速查
## §6 面试锚点（问法 | 30 秒答）
## §7 章节接口
## §8 必会·常考
## 合上书（验收对齐：默画 §2 + 故障指 §4 树）
```

**写法原则：**

| 要 | 不要 |
|----|------|
| §0 先给**位置感**（全图） | 一上来埋进细节名词 |
| 图为主，图旁人话 | 每节「打个比方 + 现场走一遍」五件套 |
| 地基挂主责章，本章地图+链接 | 为「全面」把交换机/MTU 塞进 TCP 章 |
| §2 + §4 对齐验收 | 合上书题散、验不到架构/定责 |
| 🎯🔥⭐ 外显 | 考点埋在长 prose 里 |

**密度**：不限行数，结构对优先；考点、数字、命令链路全部保留。

**网络线分工（已定案）**：地基（交换机/路由器/网卡/路由/MTU/MSS）→ **06 §1**；传输 → **08**；排障深化（route get / PMTUD / 抓包）→ **11**。

### 排版与语气（生动讲解 — 入门向，非默认）

**目标**：读者像跟着值班老手走一遍，**学得懂、记得住、面试能讲**——不是扫表格。

**每个「原理」小节（§3.x）固定五块**（顺序可微调，五块都要有）：

| 块 | 写什么 |
|----|--------|
| **一句话** | 结论先行，≤30 字 |
| **打个比方** | 1～2 句生活/游戏类比，把抽象变具体 |
| **现场走一遍** | numbered 步骤：你在哪台机器、敲什么、**屏幕出现什么**、**说明什么**、下一步去哪 |
| **输出解读** | 命令后的 `text` 块逐行/逐列解释；数字要能口算（做差、对比） |
| **面试怎么说** | 30 秒口述版，可直接背 |

**章节级写法：**

- 开篇事故场景要**有画面**（谁喊什么、常见误操作），不要一句干巴巴概括
- **概念节（§1）**：每个术语先给「值班里什么时候碰到」，再给定义表
- **架构节（§2）**：mermaid 后必须有 **读图 N 句话**，带「从哪进、往哪查」
- **命令节（§6）**：每条命令配「什么时候用、看到什么算正常/异常」
- **排障节（§7）**：用决策树 + **每条分支举一个真实现象**
- 表格是**辅助**，不能一整节只有表；表前后要有 2～4 句 prose 串起来
- 重点：`> **记住**`；易混：`> **别混**`
- 禁止：纯表格堆砌、只有术语没有场景、命令无输出、输出无解读
- 允许：短场景（3～5 句）、类比、走查叙事——**生动 ≠ 啰嗦**，不写小说

**密度**：450～650 行（轻量 350～450；K8s/中间件重章 700）；**考点、数字、命令链路全部保留**，用新讲法重新包装，不丢题

### 练习题.md

与 [`04-内核与系统/练习题.md`](01-基础底座/04-内核与系统/练习题.md) 对齐；K8s 参照 [`02-etcd/练习题.md`](03-Kubernetes/02-etcd/练习题.md)。

**固定结构：**

```markdown
# NN｜章节名 · 练习题

先对照 [知识点](知识点.md) **本章主线** 自己口述，再对答案。每题标了覆盖范围：

| 标记 | 含义 |
|------|------|
| **核心** | 不懂整章就没建立起来 |
| **必会** | 值班和面试都要能讲 |
| **常用** | 线上会反复碰到 |
| **常考** | 白板和追问的高频口径 |

## 本题目录
- [Q1 …](#q1)
- [合上书](#合上书)

---

## Q1
**★★★★★｜题干**
**覆盖**：核心 · 必会
**对应**：知识点 §3.1

### 场景
（1～3 句生产场景）

### 完整解答
**一句话**：结论先行。

（mermaid / 命令+输出 / 编号步骤）

### 再追问
- …

---

## 合上书
（与知识点「合上书」呼应，5～7 题）
```

**写法要求：**

- 开篇链到同章 `知识点.md`：运维章提示 **本章主线**；**06/09** 提示 **面试怎么考**
- 每题加 **对应**（知识点第几节）
- **完整解答** 第一句必须是 **一句话** 结论，再展开
- 排障题优先 mermaid 决策树；命令题附典型输出
- 8～12 题，覆盖该章「必会 · 常考」；保留原考点不删题
- **合上书** 与知识点自测题一致或略扩

---

## 硬约束

- 改被分配的 `知识点.md` 和/或 `练习题.md`
- 先通读旧稿再覆盖写入
- 中文正文
- 不新建、不删除、不改名章节目录
