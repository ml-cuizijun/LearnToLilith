# 全库改写标准

**样板章（结构 + **生动讲解** 对齐这两份）：**

- `02-基础底座/11-网络排障与抓包/知识点.md` — **首选样板**（场景走查 + 打个比方 + 输出解读 + 面试怎么说）
- `02-基础底座/01-内核与系统/知识点.md` — 分层排障参照
- `04-Kubernetes/02-etcd/知识点.md` — K8s / 有部署章节的参照

根 `README.md`「规范」一节同样有效。

---

## 知识点.md：固定结构

每章顶部 mermaid 导航（按主题微调第 4 步名称）：

```mermaid
flowchart LR
    a[1 概念] --> b[2 架构]
    b --> c[3 原理]
    c --> d[4 落地/部署]
    d --> e[5 配置]
    e --> f[6 命令与操作]
    f --> g[7 排障]
    g --> h[8 常考]
```

### 开篇（固定四块）

1. **事故/场景一句** — 这章解决什么生产问题
2. **本章主线（3 步）** — 读者排障/学习的主线，编号 1. 2. 3.
3. **读完你能** — 3～5 条可检验能力
4. **本章目录** — 只列一级/二级，不写「打开大纲」等 meta 说明

然后：**本章不讲**（链到其它章）→ `---` → 正文。

### 正文目录（顺序固定，不适用可整节省略）

| # | 节名 | 写法 |
|---|------|------|
| 1 | 基本概念 | 表格定义；> **记住** 一句 |
| 2 | 架构图 | mermaid + **读图 N 句话** |
| 3 | 原理 | 每小节开头 **一句话** + 图/表 + 必要时命令示例与输出 |
| 4 | 落地 / 安装部署 | 生产步骤、选型；K8s 章用「安装部署」 |
| 5 | 核心配置 | 关键配置项 + 示例 |
| 6 | 命令与操作 | 合并原「命令」「操作」；速查表 + 典型输出 |
| 7 | 生产排障 | 现象 \| 先做 \| 别做 |
| 8 | 必会 · 常考 | 对错表 + 数字 + 易混 |
| — | 合上书，问自己 | 5～7 道自测题 |

**职业发展（01）章** 可将 4→落地方法、5→红线、6→卡住时；仍保留「主线 + 一句话 + 表 + 合上书」。

**开发能力（06）章** — **不用** 9 步 mermaid 导航。按「面试 + 语法 + 原理 + 模板」：

| 节 | 内容 |
|----|------|
| 开篇 | 本章目标一句 + **面试怎么考** 表（核心/必会/常考）+ **30 秒怎么说** |
| 能力边界 | Shell/Python/Go/正则 各干什么、不干什么 |
| **语言最小集** | 读写脚本/认代码的**最小语法**（变量、循环、函数、err 处理）；不是语言大全 |
| 语法必会 | 按 SRE 场景组织：用法 + 示例 + 易错（不是语法大全） |
| 原理讲透 | 每节 **一句话** + 图；讲清「为什么这样写」 |
| 生产模板 | **完整可复制** 脚本/程序 + 典型输出 |
| 踩坑清单 | 现象 \| 原因 \| 修法 |
| 必会 · 常考 | 对错表 + 易混 |
| 合上书 | 5～6 自测 |

**AI 与智能运维（09）章** — **不用** 9 步 ops 导航。按「面试 + 边界 + 原理 + 口述」：

| 节 | 内容 |
|----|------|
| 开篇 | 加分项定位 + **面试怎么考**（问法 \| 30 秒 \| 2 分钟） |
| SRE 边界 | 该用 / 禁止（出域、自动执行、替值班） |
| 原理讲透 | 图 + 白话；拒绝论文体 |
| 落地场景 | 1～2 个值班/平台真实例子 |
| **口述模板** | 【30 秒】【2 分钟】【常见追问】— 可直接背 |
| 必会 · 常考 | 对错 + 易混 |
| 合上书 | 5～6 自测 |

> 06、09 的 `练习题.md`：口述题 + 边界判断 + 读代码/场景设计；仍保留 **对应** + **一句话**。

### 排版与语气（生动讲解，不是百科）

**目标**：读者像跟着值班老手走一遍，**学得懂、记得住、面试能讲**——不是扫表格。

**每个「原理」小节（§3.x）固定五块**（顺序可微调，五块都要有）：

| 块 | 写什么 |
|----|--------|
| **一句话** | 结论先行，≤30 字 |
| **打个比方** | 1～2 句生活/游戏类比，把抽象变具体 |
| **现场走一遍** | numbered 步骤：你在哪台机器、敲什么、**屏幕出现什么**、**说明什么**、下一步去哪 |
| **输出解读** | 命令后的 `text` 块逐行/逐列解释；数字要能口算（做差、对比） |
| **面试怎么说** | 30 秒口述版，可直接背 |

**章节级写法：**

- 开篇事故场景要**有画面**（谁喊什么、常见误操作），不要一句干巴巴概括
- **概念节（§1）**：每个术语先给「值班里什么时候碰到」，再给定义表
- **架构节（§2）**：mermaid 后必须有 **读图 N 句话**，带「从哪进、往哪查」
- **命令节（§6）**：每条命令配「什么时候用、看到什么算正常/异常」
- **排障节（§7）**：用决策树 + **每条分支举一个真实现象**
- 表格是**辅助**，不能一整节只有表；表前后要有 2～4 句 prose 串起来
- 重点：`> **记住**`；易混：`> **别混**`
- 禁止：纯表格堆砌、只有术语没有场景、命令无输出、输出无解读
- 允许：短场景（3～5 句）、类比、走查叙事——**生动 ≠ 啰嗦**，不写小说

**密度**：450～650 行（轻量 350～450；K8s/中间件重章 700）；**考点、数字、命令链路全部保留**，用新讲法重新包装，不丢题

### 练习题.md

与 [`01-内核与系统/练习题.md`](02-基础底座/01-内核与系统/练习题.md) 对齐；K8s 参照 [`02-etcd/练习题.md`](04-Kubernetes/02-etcd/练习题.md)。

**固定结构：**

```markdown
# NN｜章节名 · 练习题

先对照 [知识点](知识点.md) **本章主线** 自己口述，再对答案。每题标了覆盖范围：

| 标记 | 含义 |
|------|------|
| **核心** | 不懂整章就没建立起来 |
| **必会** | 值班和面试都要能讲 |
| **常用** | 线上会反复碰到 |
| **常考** | 白板和追问的高频口径 |

## 本题目录
- [Q1 …](#q1)
- [合上书](#合上书)

---

## Q1
**★★★★★｜题干**
**覆盖**：核心 · 必会
**对应**：知识点 §3.1

### 场景
（1～3 句生产场景）

### 完整解答
**一句话**：结论先行。

（mermaid / 命令+输出 / 编号步骤）

### 再追问
- …

---

## 合上书
（与知识点「合上书」呼应，5～7 题）
```

**写法要求：**

- 开篇链到同章 `知识点.md`：运维章提示 **本章主线**；**06/09** 提示 **面试怎么考**
- 每题加 **对应**（知识点第几节）
- **完整解答** 第一句必须是 **一句话** 结论，再展开
- 排障题优先 mermaid 决策树；命令题附典型输出
- 8～12 题，覆盖该章「必会 · 常考」；保留原考点不删题
- **合上书** 与知识点自测题一致或略扩

---

## 硬约束

- 改被分配的 `知识点.md` 和/或 `练习题.md`
- 先通读旧稿再覆盖写入
- 中文正文
- 不新建、不删除、不改名章节目录
