# 全库改写标准

**样板章（照着这两章的样子写）：**

| 类型 | 样板章 | 定位 |
|------|--------|------|
| **默认标准（全库）** | `01-基础底座/13-TCP与UDP/知识点.md` | 叙事主线 + callout 讲解 + 少表格，最完整的一版 |
| 同标准的第二例 | `01-基础底座/12-网络栈与Socket/知识点.md` | 同一套写法，稍轻量 |
| K8s 部署类 | `03-Kubernetes/02-etcd/知识点.md` | 安装部署 + 排障（骨架不同，callout 规则同样适用） |

根 `README.md`「规范」一节同样有效。

---

## 一、组织主线：按「一条东西的一生」讲，不按概念清单讲

**核心决定**：一章的骨架是一条**叙事主线**，不是概念目录。读者脑子里先有「会经历哪几个阶段」，每个机制自然挂到对应阶段，就不会乱。

| 章 | 主线 |
|----|------|
| 13-TCP与UDP | 一条**连接**的一生：选型 → 出生（握手）→ 干活（窗口/重传）→ 老去（保活/切网）→ 死亡（挥手/TW/CW）→ 另一条路（UDP/KCP/QUIC）→ 作战 |
| 12-网络栈与Socket | 一个**包**的旅程：出门 → 上路 → 进门 → 排队 → 领号 → 干活 → 回家 → 生病 |

**怎么给自己的章找主线**：问「这章的主角是什么？它从哪来、到哪去、中间会在哪卡住？」——请求的一生、一次部署的一生、一条日志的一生、一个 Pod 的一生都行。找不到叙事主线的章（工具类、参数类），才退回「概念 → 机制 → 作战」三段。

**节标题格式**：`## 三、出生：三次握手` —— **中文序号 + 阶段名 + 主题**。扫一眼就知道读到这条命的哪一段。

---

## 二、讲解载体：五种 callout 就是脊柱

不用表格当讲解主体，用 callout。读者只扫 callout 就能复述全章。

| callout | 写法 | 用在哪 |
|---------|------|--------|
| 🎯 **一句话** | `> 🎯 **一句话**：…` | **每节节首必放**，≤30 字，结论先行 |
| 💡 **打个比方** | `> 💡 **打个比方**：…` | **只给全章最难的 3～4 个概念**，1～2 句生活/游戏类比 |
| ⚠️ **别混** | `> ⚠️ **别混**：…` | 真易混处，**每对易混只写一次**，别处「见 X 节」 |
| 📝 **面试** | `> 📝 **面试**：…` | 可直接背的 30 秒口径 |
| 🔍 **现场** | `> 🔍 **现场**：…` | 值班真会敲的，**紧跟命令块** |

**难概念按需取用四层武器**，不机械套模板——难点四层全上，小概念只用前两层：

```text
① 🎯 一句话             每个概念都要
② 💡 打个比方           只给真难懂的
③ 机制怎么转            一张小图 + 读图 2~3 句
④ 🔍 现场怎么认         命令 + 真实输出 + 行内 # ← 注释
```

**术语内联定义**：**不设独立「名词扫盲」表**。术语在第一次出现处一句话讲清（`**fd（file descriptor）** = 进程里的一个小整数，指着内核里的打开对象`），读者不用来回翻。

---

## 三、排版硬规则

1. **表格 ≤ 8 张/章**。只在「真两列对比」时用表（TCP vs UDP、TW vs CW、RTO vs 快重传）。一行一句的说明性表格全部改 prose。
2. **元叙述 = 0**。**不要**：本章目录、定位表、名词扫盲表、读法说明、读图路线表、「对应阶段②」、值班签名表、顶部导航 mermaid。读者要直接落到干货上。
3. **每张图后紧跟「读图」2～3 句**，讲「从哪进、往哪查」。图不带读图句 = 白画。
4. **命令与输出永远成对**，输出行内用 `# ←` 注释读法，**不另起「输出解读」表**：
   ```text
   # LISTEN  128    128    0.0.0.0:4103   ← Recv-Q 顶到 Send-Q = 全连接队列满
   ```
5. **图 1 张主图 + 若干局部机制图**（覆盖要求见 §八 第 3 步「图清单自检」）。主图是「合上书默画」那张。mermaid **不写 `classDef fill:#...` 配色**——配色是视觉噪音，结构本身要能说话。ASCII 与 mermaid 二选一，不要同一内容两版。
6. **标记符号** 🎯重点 · 🔥难点 · ⭐常考 · 🔧实操，**只打在真考点上**，配额参考 ⭐≥5 🔧≥3 🔥≥4，不滥打。
7. **官方术语必须给足，白话只能并列旁注**。协议图、状态机、算法过程一律先写**标准写法**——报文写 `SYN=1, seq=x` / `ACK=1, seq=y, ack=x+1`，序号用真实数字或标准符号（`seq=1001`、`SND.UNA`/`SND.NXT`、`u/v/w`），**两端的状态位逐步标出**（`CLOSED → SYN_SENT → ESTABLISHED` / `CLOSED → LISTEN → SYN_RCVD → ESTABLISHED`）；白话解释放在图旁的「读图」里，**不能取代**标准写法。写成「我的序号从 x 开始」「seg0 已确认」「`[1][2][3]`」这类口语图，面试白板上一分不得。
   - **状态名两套写法要分清**：教科书/RFC 用下划线或全称（`SYN_RCVD`、`FIN_WAIT_2`、`TIME_WAIT`），Linux `ss` 输出用连字符缩写（`SYN-RECV`、`ESTAB`、`TIME-WAIT`）。正文/图里写教科书名，命令输出块里保留工具名，第一次并列出现时用一条 `⚠️ 别混` 交代清楚。
8. **CJK 不进 ASCII 框线图**。中文是宽字符，`┌─┐` 是窄字符，任何等宽字体都对不齐——状态机、时序这类必须对齐的图用 mermaid，ASCII 只画「左对齐 + 箭头」这种不需要收框的图。
9. **关键数字集中一处**：收口节一张「数字墙」引用块，正文顺带提到即可，不各处重复。
10. **开篇只要 3 行**：① 事故场景（谁喊什么、常见误操作，要有画面）② 本章回答什么 ③ 本章不讲什么 + 链出去。然后 `**标记**：…` 一行 → `---` → 直接进正文。

---

## 四、知识点.md 骨架

```markdown
# 章节名

> 玩家 A 发技能不触发、新玩家连不上——`ss` 上 ESTAB 一排排，问题却不在业务代码。
> **本章回答**：（一句，这章串起来的那条线）
> **不讲**：X → [NN](../NN-xxx/)；Y → [MM](../MM-xxx/)。

**标记**：🎯重点 · 🔥难点 · ⭐常考 · 🔧实操

---

## 一、一张图：<主角>的一生 / 旅程
> 🎯 **一句话**：…
（主图 mermaid）
**读图**：2～3 句，含「ss/监控照的是哪几段」

## 二 ～ N、按主线分阶段
每节：🎯 一句话 → 机制（图 + 读图 / prose）→ ⚠️ 别混 → 🔍 现场（命令+输出）
难点加 💡 打个比方，考点加 📝 面试

### （最厚的那一节末尾）收束：<主角>凭什么<核心特性>
把本节散落的机制收成**一张结构图 + 一条可背清单**（样板：13 章「TCP 凭什么可靠」七件套）
必配：读图（分组解释）+ ⚠️ 别混（讲清这个特性**不保证**什么）+ 📝 面试（给记忆分组法）

## 倒数第二节、作战：定责
- 决策树 mermaid（每条分支写真实现象）
- 现象 · 先做 · 别做（这是该用表的地方）
- 60 秒剧本 🔧：接到报障 0–10s / 10–30s / 30–45s / 45–60s 各敲什么

## 最后一节、收口
- 命令速查 🔧
- 面试锚点表（问 | 30 秒答）
- 易错一句话对照（`错讲法 → 对讲法` bullet 列表）
- 数字墙（一个引用块）

## 合上书
5～12 条「口述/默画」验收，**每个主线阶段至少一条、逐条对得上正文小节**；
需要白板画的阶段（握手/挥手/队列/状态机）写成「**默画**…」并点明要标出的报文字段与状态位
```

**密度参考**：以 13 章为标尺（现 ~790 行），**550～800 行，prose 为主**；800 是硬顶，超了就该拆章或砍重复，不是继续加。判据不是行数而是「删掉任一句都会丢考点」——讲透，但禁止注水、禁止同一 FAQ 在多节重复。

**要 / 不要**：

| 要 | 不要（旧标准里已废弃的写法） |
|----|------|
| 叙事主线，机制挂到阶段上 | 概念清单式目录 |
| callout 当讲解脊柱 | 表格堆砌，一整节只有表 |
| 每节 🎯 一句话开头 | 每个原理小节都套「一句话/比方/走一遍/输出解读/面试怎么说」五块 |
| 难点才给比方 | 小概念也套「①例子②定义③命令④易错」四步 |
| 3 行开篇直接进正文 | 顶部 8 节点 mermaid 导航 + 开篇固定四块 + 本章目录 |
| 术语首次出现处内联定义 | 独立名词扫盲表 |
| 🎯💡⚠️📝🔍 五种 callout | `> **记住**` / `> **别混**` 旧写法 |
| 一张主图 + 局部机制图 | 6-subgraph + 12 个 classDef 配色的总图 |
| 地基挂主责章，本章给地图+链接 | 为「全面」把交换机/MTU 塞进 TCP 章 |

**网络线分工（已定案）**：网卡之后到进程之间（分层封装/路由/ARP/软中断/队列/Socket/fd/epoll/Recv-Q）→ **12**；传输层（握手/窗口/重传/拥塞/心跳/挥手/UDP/KCP/QUIC）→ **13**；抓包手法、MTU/PMTUD 深排障 → **16**；conntrack → **17**；软中断 CPU 侧 → **06**。

---

## 五、两类特例章（骨架不同，第二/三节的 callout 与排版规则照样适用）

**开发能力（05）章** — 按「面试 + 语法 + 原理 + 模板」：

| 节 | 内容 |
|----|------|
| 开篇 | 本章目标一句 + **面试怎么考** 表（核心/必会/常考）+ **30 秒怎么说** |
| 能力边界 | Shell/Python/Go/正则 各干什么、不干什么 |
| **语言最小集** | 读写脚本/认代码的**最小语法**（变量、循环、函数、err 处理）；不是语言大全 |
| 语法必会 | 按 SRE 场景组织：用法 + 示例 + 易错（不是语法大全） |
| 原理讲透 | 每节 🎯 一句话 + 图；讲清「为什么这样写」 |
| 生产模板 | **完整可复制** 脚本/程序 + 典型输出 |
| 踩坑清单 | 现象 \| 原因 \| 修法 |
| 必会 · 常考 | 对错表 + 易混 |
| 合上书 | 5～6 自测 |

**AI 与智能运维（08）章** — 按「面试 + 边界 + 原理 + 口述」：

| 节 | 内容 |
|----|------|
| 开篇 | 加分项定位 + **面试怎么考**（问法 \| 30 秒 \| 2 分钟） |
| SRE 边界 | 该用 / 禁止（出域、自动执行、替值班） |
| 原理讲透 | 图 + 白话；拒绝论文体 |
| 落地场景 | 1～2 个值班/平台真实例子 |
| **口述模板** | 【30 秒】【2 分钟】【常见追问】— 可直接背 |
| 必会 · 常考 | 对错 + 易混 |
| 合上书 | 5～6 自测 |

> 05、08 的 `练习题.md`：口述题 + 边界判断 + 读代码/场景设计；仍保留 **对应** + **一句话**。

---

## 六、练习题.md

**样板**：`01-基础底座/13-TCP与UDP/练习题.md`、`01-基础底座/12-网络栈与Socket/练习题.md`。

```markdown
# NN｜章节名 · 练习题

先合上知识点，对着 **一节的主图** 把 <主线> 口述一遍，再来做题。

| 标记 | 含义 |
|------|------|
| **核心** | 不懂整章就没建立起来 |
| **必会** | 值班和面试都要能讲 |
| **常用** | 线上会反复碰到 |
| **常考** | 白板和追问的高频口径 |

## 本题目录
- [Q1 …](#q1)
- [合上书](#合上书)

---

## Q1

**★★★★★｜题干（用面试官会问的原话）**

**覆盖**：核心 · 必会 · 常考 ｜ **对应**：知识点 [三、出生：三次握手](知识点.md)

### 场景
（1～3 句生产场景，带上「有人准备怎么误操作」）

### 完整解答
> 🎯 **一句话**：结论先行，≤40 字。

（prose 讲透机制 / 图 / 命令+行内 `# ←` 注释）

> ⚠️ **别混**：… （易混题必给）
> 📝 **面试**：… （常考题给可背口径）

### 再追问
（1～2 个追问 + 答案，答案里要有「不要怎么做」）

---

## 合上书
（逐条对齐知识点的「合上书」，条数一致或略扩）
```

**写法要求：**

- **对应** 写成 `知识点 [中文序号节名](知识点.md)`——**节名照抄正文标题**。别写「§6.2」这种编号（一改结构就全错），也别拼中文锚点 `#三出生三次握手`（带空格/斜杠的标题锚点各渲染器不一致，容易点空）
- 题干用面试官原话；**完整解答第一句必须是 🎯 一句话**
- 知识点里的五种 callout 在练习题里同样用（🎯 必给，⚠️📝 按题型给）
- 排障题优先 mermaid 决策树；**mermaid 不写 classDef 配色**
- 命令题必附典型输出，输出行内 `# ←` 注释
- 命令口径与知识点保持一致（例：队列溢出统一用 `nstat -az | grep -iE 'ListenOverflows|ListenDrops'` + 连采两次看差值，不用 `netstat -s`）
- 跨章引用写清章号并链接，改章号时全库同步
- **10～14 题**，覆盖该章「必会 · 常考」；**改写不删原考点**，知识点新增的考点要补题
- **合上书** 与知识点「合上书」逐条对齐

---

## 七、目录与编号约定

**十个大章（章号是全库引用的锚，改一次全库要跟着改）：**

| 章号 | 目录 | 章号 | 目录 |
|------|------|------|------|
| **01** | 基础底座 | **06** | 云平台 |
| **02** | 中间件与数据库（**两级**，见下） | **07** | 游戏运维专题 |
| **03** | Kubernetes | **08** | AI与智能运维 |
| **04** | 工程化与自动化 | **09** | 可观测性与SRE方法论 |
| **05** | 开发能力（**两级**，见下） | **10** | 面试综合与系统设计 |

**每个子目录固定两个文件**：`知识点.md` + `练习题.md`，不多不少。

**跨章引用写法**（开篇「不讲」行和正文链接统一用这套）：

```markdown
同章内：     [16](../16-网络排障与抓包/)                       ← 只写子目录号
跨章：       [03-04](../../03-Kubernetes/04-Pod生命周期/)      ← 章号-子目录号
带名字：     [02-01 MySQL](../../02-中间件与数据库/01-MySQL/)  ← 名字可选，号必须对
```

### 02 / 05 两章例外：两级编号

**除 02、05 两章外，所有章都是一层扁平编号。** 这两章的一级目录都不是「知识点」，而是**同一考点族的容器**：

- **05 开发能力**：一级按语言分家。扁平铺开看不出某个考点归哪门语言（`27-错误处理与panic` 是 Go 还是 Python？）。
- **02 中间件与数据库**：MySQL / Redis 各自的考点量顶得上一个小模块，扁平铺开会出现 `10-MySQL索引与执行计划` 这种「名字里再背一遍归属」的编号。

```
05-开发能力/01-Shell/01~12   ·   02-Python/01~14   ·   03-Go/01~15
           04-正则与文本处理/（无二级）   ·   05-运维开发实战题/（无二级）   ·   06-SRE手写题与算法轮/（无二级）

02-中间件与数据库/01-MySQL/01~05   ·   02-Redis/01~04
                 03-Kafka ~ 09-API网关与服务治理（均无二级）
```

引用写法随之分三种：

```markdown
指两级章的二级：  [05-03-08](../../05-开发能力/03-Go/08-context取消与超时传播/)   ← 章-容器-子目录
                  [02-01-02](../../02-中间件与数据库/01-MySQL/02-索引与执行计划/)
指同章的一级：    [05-04](../../05-开发能力/04-正则与文本处理/)                    ← 仍是两段式
                  [02-03](../../02-中间件与数据库/03-Kafka/)
两级章内同容器：  [06](../06-管道重定向与PIPESTATUS/)                              ← 只写子目录号
```

**别再新增两级章。** 只有「一级是考点族容器」这一种理由值得多一层；按主题细分请开新的一级章。

**编号 = 阅读顺序。** 每个模块内部按「基础在前、同类相邻」排；新增章要**插到它该在的位置并重排后续编号**，
不许为省事追加到尾部。重排后必须全库 grep 校验标签与路径一致（见下条）。

**编号必须和路径一致**——写 `[04-工程化](../../04-工程化与自动化/)` 这种「标签是旧编号、路径是新目录」的，读者按号找章会找错。改动目录名或编号时，先 grep 全库把所有引用的**标签和路径一起**改掉。

---

## 八、改写流程与交付自检

一章的改写按这五步走，**每步不过不许进下一步**。

### 第 1 步：定主线与节名

写下这一章的主角、它的生命阶段、每节的 `## 中文序号、阶段名：主题`。**主线定不下来别动笔**——先补主线，否则写出来一定是概念清单。

### 第 2 步：官方术语齐全性自检（grep，最容易漏的一步）🔥

先列出「面试官会用哪些**官方名词**问这一章」，然后**逐个 grep 计数**，凡是计数为 0 的都是漏考点：

```bash
cd <章目录>
for w in 流量控制 拥塞控制 可靠传输 半打开 PAWS OSI 七层; do
  printf '%-12s %s\n' "$w" "$(grep -c "$w" 知识点.md || echo 0)"
done
```

- **计数 0 = 这一章答不了那道题**，不管正文把机制讲得多细。13 章曾把 `rwnd`/`cwnd` 讲透却从没写下「流量控制 / 拥塞控制」，12 章曾画了四层封装图却全库 `OSI` 出现 **0** 次——都是这一步抓出来的。
- 术语表怎么列：翻**该领域最高频的八股题题干**，题干里的名词就是必须出现的词。
- 只补**真会被问到的官方名**；为了「全面」硬塞冷门机制（TCP Fast Open、糊涂窗口综合症）就是注水。
- **官方名 + 英文原名 + 白话**三件一起给：`**流量控制（flow control）**`，后面再跟白话。

### 第 3 步：图清单自检 🔥

**先列图清单再画图**，不是想到哪画到哪。一章的图必须覆盖下面四类，缺一类就是没讲透：

| 类 | 要求 | 例（12 / 13 章） |
|----|------|------|
| **① 主图** | **只 1 张**，就是「合上书默画」那张，画全生命阶段 | 一个包的旅程 / 一条连接的一生 |
| **② 过程图** | 每个「有先后顺序」的机制各 1 张，报文字段与两端状态位齐全 | 三次握手 / 四次挥手 / 窗口滑动三帧 |
| **③ 结构图** | 收束节那张，把散机制收成一张可背清单 | TCP 可靠性七件套 / OSI ↔ TCP-IP 分层映射 |
| **④ 决策树** | 作战节 1 张，每条分支写**真实现象**不写抽象类别 | 四个「满」定责 |

- **mermaid 还是 ASCII**：需要**对齐**的（时序、状态机、结构树、带中文的分支）一律 mermaid；只有「左对齐 + 箭头、不收框」的示意图才用 ASCII（规则 ⑧：CJK 不进 `┌─┐`）。
- **每张图后紧跟「读图」2～3 句**，且读图要讲「**从哪进、往哪查**」，不是复述图里的字。
- 图不是越多越好：**同一机制不出两版图**；一张图讲不清就是机制没想清楚，不是要再补一张。

### 第 4 步：知识体系脉络自检

读者读完要能自己串起来，靠三个东西，缺一个就是「知识点齐了但脉络断了」：

1. **一节的主图** —— 先给全局，后面每节都能挂上去。
2. **节名自带叙事** —— 只看目录就知道读到主角的哪一段。
3. **收束节** —— 最厚那节末尾必须有一个 `### 收束：…`，把该节所有机制收成**一条能背的清单**（结构图 + 分组读图 + 「不保证什么」的别混 + 记忆分组的面试口径）。

> 判据：**合上书只记住主图和收束清单，能不能把整章复述个八成？**能，脉络就立住了。

### 第 5 步：交付前逐条打勾

```text
结构
[ ] 开篇只 3 行（场景 / 本章回答 / 不讲+链接），链接标签与路径编号一致
[ ] 节名 = 中文序号 + 阶段名 + 主题；一节是主图，倒数第二节作战，最后一节收口
[ ] 最厚的节有「收束」小节
[ ] 元叙述 = 0（无目录/定位表/名词扫盲表/读法说明）

内容
[ ] 第 2 步的术语 grep 全部 > 0
[ ] 官方写法优先：报文 `SYN=1, seq=x` / `ack=x+1`，两端状态位逐步标出
[ ] 教科书名 vs 工具输出名（`SYN_RCVD` vs `SYN-RECV`）用一条 ⚠️ 别混 交代
[ ] 术语首次出现处内联定义，不设名词表

图与排版
[ ] 图清单四类齐（主图 1 张 · 过程图 · 结构图 · 决策树），每张后有读图
[ ] mermaid 无 classDef；ASCII 里无中文框线
[ ] 表格 ≤ 8 张，且每张都是「真两列对比」
[ ] 命令与输出成对，输出行内 `# ←` 注释
[ ] ⭐≥5 🔧≥3 🔥≥4，每节节首有 🎯 一句话
[ ] 行数 550～800

收口与联动
[ ] 收口节：命令速查 / 面试锚点表 / 易错一句话对照 / 数字墙
[ ] 新增的每个考点都同时进了「面试锚点」和「易错对照」
[ ] 合上书 5～12 条，每个主线阶段至少一条
[ ] 练习题.md 同步：10～14 题；题数已满时把新考点折进已有题的「### 再追问」，不删原考点
[ ] 练习题「合上书」与知识点「合上书」逐条对齐
```

> **章已有内容 → 改写不是重写**：先通读旧稿，把旧稿里所有考点列出来，改完逐条核对**一个都没丢**——旧稿的考点是底线，新标准只加不减。
>
> **章是空壳（首行写着「待写」）→ 从零起稿**：没有旧稿可对，考点底线换成**第 2 步的术语清单**——先把八股题题干里的官方名词列全，再逐个写到清单清零。

---

## 硬约束

- 改被分配的 `知识点.md` 和/或 `练习题.md`
- 有旧稿的章先通读旧稿再覆盖写入；空壳章直接按 §八 第 2 步的术语清单起稿
- 中文正文
- **不擅自新建/删除/改名章节目录**——目录结构调整属于全库改动（会连带改动所有引用的标签与路径），只在明确要求下做，做完必须全库校验链接与编号一致
